---
import { getDictionary, toLocale } from '../i18n';

const t = getDictionary(toLocale(Astro.currentLocale)).marquee;
const items = [t.items.free, t.items.noMoney, t.items.friends, t.items.rugby];
---

<section aria-label={t.label}>
    <div class="track">
        {
            [false, true].map((clone) => (
                <div class="group" aria-hidden={clone || undefined}>
                    {items.map((item) => (
                        <>
                            <span>{item}</span>
                            <span class="dot">●</span>
                        </>
                    ))}
                </div>
            ))
        }
    </div>
</section>

<style>
    section {
        background: var(--text);
        color: var(--bg);
        overflow: hidden;
        border-top: 1px solid var(--text);
        border-bottom: 1px solid var(--text);
    }

    .track {
        display: flex;
        width: max-content;
        animation: marq 32s linear infinite;
        padding: 18px 0;
    }

    .group {
        display: flex;
        align-items: center;
        gap: 34px;
        padding-right: 34px;
        font-family: var(--font-display);
        font-size: 30px;
        letter-spacing: 0.02em;
        white-space: nowrap;
    }

    .dot {
        color: var(--accent);
    }

    @keyframes marq {
        from {
            transform: translateX(0);
        }
        to {
            transform: translateX(-50%);
        }
    }

    @media (prefers-reduced-motion: reduce) {
        .track {
            animation: none;
        }
    }
</style>
