# Fichier versionné exprès : @expo/fingerprint hache le .gitignore du projet,
# et expo start le crée tout seul s'il manque (bloc ci-dessous). Absent du dépôt,
# il existerait sur la machine locale et pas sur EAS : deux empreintes différentes
# pour le même commit. Les règles communes vivent dans le .gitignore racine,
# qui, lui, n'entre pas dans l'empreinte.

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli