# TryCast — Dashboard

> Suivi d'avancement et décisions. Mis à jour à la fin de chaque session.
> Dernière mise à jour : **2026-07-11 (après-midi)**

## Avancement des lots

| Lot | Sujet | État |
|-----|-------|------|
| 0-1 | Fondations, auth/profils, delete-account | ✅ Livré |
| 2 | Pipeline compétition (sync-fixtures, cron) | ✅ Livré |
| 3 | Prédictions + RLS deadline kickoff, pages Matchs/Résultats | ✅ Livré |
| 4 | Scoring (RPC `apply_match_scores`, EF `sync-results`, passes 1/2) | ✅ Livré, validé en réel J1 NC |
| 5 | Ligues & classements (standings, RPC, leaderboards, Realtime) | ✅ Livré |
| 5.5 | Design system + i18n (tokens light/dark, primitives, 4 onglets, i18next FR) | ✅ **Livré (V0 + correctifs retours)** |
| **6** | **Push (expo-notifications, tokens, EF notify, deep links, préférences)** | 🔶 **Code livré — déploiement EF + dev build Android + test device à faire (Corentin)** |
| 7 | Finitions (confirmation email, EN si beta anglophone, pages légales, etc.) | ⬜ À venir |

## Ce qu'il reste à faire

### Lot 6 — Push : reste la mise en route (actions Corentin, détail dans « En attente côté Corentin »)
Code entièrement livré le 2026-07-11 : tables `push_tokens`/`notification_prefs`/`notification_sends` + RPC (poussées en dev), EF `notify` unique (rappels H-1 + résultats passe 1, claim-first idempotent, contenus localisés via `profiles.locale`), cron `3-53/10` (migration **pas encore poussée** — deploy EF d'abord), enregistrement du token au login, deep links au tap (allowlist → onglets Matchs/Résultats), section Notifications des Réglages (master + 2 toggles + bannière permission). **Android d'abord** (décision 2026-07-11 : téléphone perso, pas de compte Apple payant) ; iOS/APNs au Lot 7. Types « Activité de ligue » et « Invitations » : ni toggle ni backend (v1.1+, schéma prefs extensible).

### Écrans : blocs maquettés en attente de backend (reportés du Lot 5.5)

*Les maquettes de `docs/design/` vont plus loin que ce que les données permettent aujourd'hui. Ce qui suit est **prêt côté design** (et souvent côté clés i18n) mais bloqué par du serveur/data à construire — chaque bloc liste son préalable.*

**Accueil Matchs** (`MesMatchs.dc.html`)
- **Carte LIVE** (minute de jeu, score en cours, points potentiels épinglés en haut) → Highlightly ne fournit **pas de score pendant le match** (`state.score` = null avant/pendant, constaté sur réponses réelles). Préalable : autre source live ou changement de provider — à réévaluer seulement si le besoin devient réel (décision spike 2026-07-05).
- **Barres de distribution des pronos 1/N/2** (répartition de la communauté sous les points potentiels) → agrégat serveur à créer (RPC/vue `security definer` sur `predictions`), **à n'exposer qu'après kickoff** pour ne pas fuiter les pronos (la RLS actuelle interdit — à raison — de lire les pronos des autres avant).

**Résultats / Match Detail** (`TryCast Match Detail.dc.html` — écran entier reporté)
- **Timeline du match** (essais, transformations, pénalités, cartons, minute par minute) → données événements **inexistantes chez tous les providers testés** (spike clos, `docs/spike-highlightly.md`). Pas de préalable atteignable au MVP ; l'écran Résultats reste la vue « score + réconciliation du prono ».

**Profil** (`TryCast Profil.dc.html`) — refondu le 2026-07-09 (header + chiffres clés, sélecteur de compétition, tabs Stats/Pronos/Ligues, donut de précision, rang au général). Reste bloqué par le backend :
- **Courbe « Points par journée »** (onglet Stats) → série de points par journée côté serveur (agrégat sur `predictions` scorées par jour) ; état vide assumé en attendant.
- **Photo de profil** (Changer/Supprimer, maquette Réglages) → bucket Supabase Storage + colonne `profiles.avatar_url` + policies (+ redimensionnement côté EF si on veut faire propre).

**Réglages** (`TryCast Reglages.dc.html` — livré en V0 minimal : thème, langue affichée, version, déconnexion, zone danger)
- ~~**Notifications** (master + par type + bannière « bloquées dans iOS »)~~ → **livré au Lot 6** (2 types câblés ; « Activité de ligue » et « Invitations » suivront leur backend).
- **Changement d'e-mail** (ligne « Modifier ») et **changement de mot de passe** (sheet maquettée avec erreurs) → `supabase.auth.updateUser` + e-mails de confirmation ; à câbler en même temps que la réactivation de la confirmation email (Lot 7).
- **RGPD** : sheet consentements (mesure d'audience, communications, avec dates de recueil) + « Exporter mes données » → table `consents` + EF d'export (envoi par e-mail) ; n'a de sens qu'une fois un analytics en place.
- **Liens légaux** (CGU, politique de confidentialité, mentions légales) → pages à rédiger et héberger (requis App Store — Lot 7). Le footer légal de la maquette Auth attend les mêmes pages.
- **Sélecteur de langue actif** (aujourd'hui « Français » en lecture seule) → livraison de l'EN (infra prête, traduction avant RWC 2027).
- **Compte social-only** (« Géré par Apple », pas de mot de passe) → OAuth Apple/Google (v2), comme les boutons « Continuer avec Apple/Google » de la maquette Auth.

### En attente côté Corentin (actions manuelles)
- [ ] **Mise en route du Lot 6** (dans cet ordre) :
  1. Credentials FCM : `npx eas-cli credentials -p android` → Google Service Account Key for Push Notifications (FCM V1) → upload du JSON de la clé privée Firebase (déjà générée)
  2. `npx eas-cli env:create --scope project --name GOOGLE_SERVICES_JSON --type file --value ./google-services.json --environment development`
  3. `supabase secrets set NOTIFY_SECRET=<valeur>` puis `supabase functions deploy notify`
  4. SQL editor : `select vault.create_secret('<même valeur>', 'notify_secret');` puis `supabase db push` (applique la migration cron `20260711000300` — **jamais avant le deploy**)
  5. Dev build : `npx eas-cli build --profile development --platform android` → installer l'APK, lancer avec `npx expo start --dev-client`
  6. Checklist device : permission demandée 1 fois au premier lancement connecté ; token visible dans `push_tokens` ; couper la permission OS → bannière + master off dans Réglages ; rappel H-1 reçu sur un match sans prono, pas de re-rappel au tick suivant, plus de rappel après avoir pronostiqué ; push résultats après passe 1 avec les bons points, rien à la passe 2 ; tap rappel → onglet Matchs, tap résultat → onglet Résultats (app tuée et en arrière-plan) ; déconnexion → plus de push. Test isolé possible avant l'EF : token loggé en dev → https://expo.dev/notifications
- [ ] Saisie admin des essais J1 (`scripts/admin-set-tries.sql`) → déclenche passe 2 auto au tick suivant
- [ ] Test live complet sur la **J2 du NC le 2026-07-11** (valide standings + Realtime en réel ; pronos déjà saisis) — **et premier test réel du nouveau DS sur mobile**
- [ ] Renouvellement de l'abonnement odds **Highlightly Pro** (échéance ~2026-08-06) : décider avant chaque compétition (6N, RWC) ; sans lui, `/odds` répond 401 et on retombe sur le fallback ×2.0
- [ ] Réévaluer la confirmation email (désactivée) au Lot 7

## Décisions clés (actées, ne pas re-débattre)

*Détail complet dans la mémoire projet (`trycast-project-charter`). Rappel des plus structurantes :*

- **Prono unique** par (user, match), partagé entre ligues ; points rétroactifs en rejoignant.
- Saisie = **score exact** + cases bonus offensif ; points vainqueur **pondérés par les cotes** (fallback 2.0), barème versionné en DB (`scoring_rules`).
- Deadline prono au **kickoff**, imposée par **RLS serveur**.
- **Scoring 2 temps** : passe 1 immédiate sans bonus offensif, passe 2 après saisie admin des essais (aucun fournisseur ne les expose — définitif MVP).
- Logique de scoring en **module TS pur** + écriture atomique via **une seule RPC** `apply_match_scores`.
- Source de données : **Highlightly** (API-Sports, Sportradar, etc. écartés).
- **4 onglets** (Lot 5.5, remplace les 5 du Lot 5) : Matchs / Résultats / Classement / Profil — les ligues sont ventilées (CTAs sur l'accueil, bascule Ligues/Général dans Classement, gestion via le détail de ligue).
- **Auto-save optimiste du prono** (Lot 5.5, remplace le bouton Valider) : statut À pronostiquer → Enregistrement… → Enregistré dans la carte.
- Ligues : création/adhésion **uniquement par RPC** (code d'invitation jamais requêtable) ; owner supprime son compte → sa ligue disparaît en cascade (transfert = v2) ; l'owner ne quitte pas sa ligue, il la supprime.
- **i18n : FR langue source via i18next** (clés typées, namespaces par domaine), `profiles.locale` synchronisée au démarrage pour les notifications du Lot 6 ; EN livré au plus tard avant la RWC 2027.
- **Design system** : identité vert bouteille/crème/grenat (« le grenat est une étincelle, jamais un fond »), Anton + Inter, light + dark complets. Référence : `docs/design/` + skill `trycast-design-system`.

## Points ouverts / dette assumée
- App lit encore `BAREME_V1` hardcodé pour l'aperçu → **brancher l'app sur la DB avant de créer une v2** du barème.
- Override npm `react-native-css` → `lightningcss@1.30.1` (bug bundling natif) — à réévaluer quand react-native-css > 3.0.7 sort.
- **Contrainte tokens natifs** (découverte 2026-07-09) : react-native-css n'enregistre pas les variables `:root`/`@theme` au runtime → les `--color-*` de `global.css` doivent rester des littéraux ou `light-dark(hex, hex)` sans `var()` interne ; hex dupliqués dans `src/tw/palette.ts` (couleurs lues en JS via `useThemeColor`), synchronisation verrouillée par `src/tw/palette.test.ts`.
- Barre « moi » épinglée du Classement : le rang du joueur au-dessus est approximé à rang − 1 (exact hors ex æquo exotiques, documenté dans `pinned-me-row.tsx`).
- **Plancher browserslist obligatoire** (fix 2026-07-10) : le champ `browserslist` de `package.json` (Chrome 123 / Safari 17.5 / Firefox 120 minimum) garantit que `light-dark()` survit à la passe Lightning CSS d'`@expo/metro-config` — le retirer ou le baisser re-casse silencieusement tout le dark natif. La bascule manuelle web passe par `color-scheme` sur `:root` (`theme-preference.ts`), plus de dépendance au « space toggle » `--lightningcss-*`.
- Tailwind : la palette par défaut (`bg-blue-600`…) reste techniquement disponible — la neutraliser (`--color-*: initial`) maintenant que tous les écrans sont sur les tokens.
- Identifiants encore hardcodés (signalés) : `package.json` (typegen `--project-id`), `app.json` (owner EAS, requis), migration cron `20260705000300` (URL projet).
- Base dev : 6 matchs J1 NC ont `scored_at` mais 0 prono scoré → classement général NC vide (état donnée, pas un bug).

## Journal des sessions
- **2026-07-11 (bis)** — **Lot 6 livré (code) : notifications push, Android d'abord** (13 commits, plan `~/.claude/plans/parfait-tout-a-maintenant-resilient-ritchie.md`). Décision : **Android en premier** (téléphone perso de Corentin, pas de compte Apple Developer payant, APK direct) — Firebase `trycast-90446` créé par Corentin, package `com.cohozen.trycast` ; iOS/APNs au Lot 7. Backend : tables `push_tokens` (unicité sur le token, écriture **par RPC security definer** `register/unregister_push_token` — l'upsert PostgREST ne peut pas réaffecter la ligne d'un autre compte sous RLS, cas « changement de compte sur le même téléphone »), `notification_prefs` (booléens par type, absence de ligne = tout activé), `notification_sends` (dédup **claim-first** sur unique (user, match, type) : crash = notif perdue, jamais doublée ; supporte les receipts Expo) ; RPC `notify_reminder_targets`/`notify_result_targets` (SQL versionné, service_role only) ; **une seule EF `notify`** scannante (receipts → rappels H-1 → résultats passe 1, tokens morts DeviceNotRegistered purgés aux deux niveaux, `job_runs` avec early-exit) sur cron `3-53/10 * * * *` (décalé de 3 min après sync-results ; migration cron commitée **non poussée** — deploy EF d'abord). App : `expo-notifications` + `app.config.ts` (googleServicesFile via env EAS, fichier gitignoré), `eas.json`, feature `src/features/notifications/` (garde-fou `push-support` — Expo Go/simulateur/web court-circuités —, permission demandée une fois au premier lancement connecté, token désenregistré avant signOut), deep links au tap via **allowlist** `data.url` → onglets Matchs/Résultats (`getLastNotificationResponseAsync` + listener), primitive `ui/Switch` (46×28), section Notifications des Réglages (master + sous-titre d'état + 2 toggles + bannière permission refusée avec `Linking.openSettings()`, re-check au retour au premier plan, upsert optimiste). Vérifié : suite complète (**204 tests**, +24), `e2e-notifications.sh` **11/11 vert contre le dev** (réaffectation de token, isolation RLS, tables/RPC serveur verrouillées), advisors = pattern assumé uniquement, `expo export` iOS OK, simulateur iPhone 17 Pro (section en dark **et** light, master off → sous-toggles masqués + `master=false` en base, état de Corentin restauré : thème Sombre, prefs tout-ON, 0 token). Reste : mise en route par Corentin (checklist dans « En attente »).
- **2026-07-11** — **Troisième série de retours Lot 5.5** (6 retours, dernière passe avant le Lot 6 ; 4 commits). Résultats : la bande de jours présélectionne le **jour avec matchs le plus proche d'aujourd'hui** (dernier de la plage, bornée à today) et se positionne dessus au montage (`contentOffset` calculé depuis la géométrie des pilules) ; la modale de détail des points affiche la rangée **« Points de base · 1 N 2 »** vue pendant les pronos (`winnerPointsByOutcome`, issue pronostiquée encadrée grenat) au-dessus du barème. Profil : `computeProfileStats` reçoit désormais les matchs terminés — les **non pronostiqués comptent au dénominateur de la précision** (choix Corentin : catégorie dédiée, segment neutre `text-faint` dans le donut + 4ᵉ ligne de légende, `missed` reste distinct) ; l'onglet Pronos liste **tous les matchs terminés** y compris sans prono (ResultCard gérait déjà le cas) ; bouton réglages sur la **même row que avatar/pseudo** (visible aussi pendant le skeleton) ; petit `pb-2` sous le bloc épinglé des tabs. Tests de `compute-profile-stats` réécrits (Map + matchs, 5 cas). Vérifié : suite complète (180 tests) + simulateur iPhone 17 Pro en **light et dark** (strip sur SAM 11, modale cohérente carte prono — ×1,32 → 13 pts, donut 25 % = 2 bons / 8, les 6 matchs du 4 juillet en « non pronostiqué » listés) ; thème « Sombre » de Corentin restauré après le test light.
- **2026-07-10 (sexies)** — **Fix : warning react-native-css « added or removed a variable » au press des boutons** (remonté par Corentin sur créer/rejoindre ligue). Cause : les classes ajoutées conditionnellement au press (`scale-[0.97]`, `shadow-none`…) compilent en variables CSS (`--tw-scale-*`, `--tw-shadow`) ; react-native-css fige au premier rendu le statut « porteur de variables » d'un composant, et quand il bascule au press, il **démonte/remonte le composant et ses enfants** (état perdu + warning dev). Fix : classe runtime `will-change-variable` (fournie par react-native-css, inerte sur web) dans les classes de base du `Pressable` des 3 primitives concernées (`button`, `icon-button`, `chip`) — le statut ne bascule plus jamais. Piège documenté dans le skill DS. Vérifié : suite complète (177 tests) + simulateur iPhone 17 Pro (press réel d'un `Button` actif : zéro warning dans Metro, feedback rétrécissement/assombrissement intact — screenshot en plein press). Note : `format:check` échoue sur `src/app/(app)/(tabs)/index.tsx`, fichier en cours de modif par Corentin (hors périmètre, non touché).
- **2026-07-10 (quinquies)** — **Safe areas généralisées** (suite du retour tab bar) : hook `useScreenInsets()` dans `src/tw/use-screen-insets.ts` — `top` = `max(inset haut, 56)` (remplace tous les `pt-14`), `bottomTabBar` = `94 + max(inset bas, 16)` (remplace tous les `pb-32`) ; planchers = anciennes valeurs fixes, donc rendu identique sur les appareils déjà calibrés, et plus de risque de chevauchement sur les barres d'état Android hautes. Migré : les 4 écrans à onglets (y c. états loading), Réglages, les 2 écrans auth, la primitive `Screen` ; les écrans `league/*` gardent leur header natif (safe area déjà gérée par le Stack). **Piège react-native-css découvert en route** : hors cible `style`, une prop inline écrase le style dérivé des classes — passer `contentContainerStyle` faisait perdre tout le `contentContainerClassName` (px-5/gap/max-w envolés au premier essai). Fix centralisé : le wrapper `ScrollView` de `@/tw` fusionne lui-même classes résolues + inline (garde-fou dev si l'élément est enveloppé par un provider). Documenté dans le skill DS. Vérifié : suite complète (177 tests) + parcours simulateur iPhone 17 Pro (5 écrans, sticky et réserve basse intacts) ; pas de simulateur SE sous iOS 26 — planchers garantis par construction (`Math.max`), Android à valider sur appareil réel.
- **2026-07-10 (quater)** — **Deuxième série de retours Lot 5.5** (6 retours d'ergonomie, 5 commits). Auth : contenu aligné en haut (`justify-center` retiré — le switch Connexion/Inscription ne fait plus sauter logo + tabs) et champ actif maintenu visible au-dessus du clavier (`KeyboardAvoidingView` remplacé par `automaticallyAdjustKeyboardInsets` sur le ScrollView ; Android compte sur le mode `resize` par défaut — **smoke test Android à faire par Corentin**). Tab bar flottante décollée de la barre système via `useSafeAreaInsets` (premier usage de safe-area-context du projet ; provider déjà fourni par expo-router ; plancher 16px conservé). « Mes matchs » : en-tête + carte stats épinglés (motif `View flex-1` > bloc `flex-none` > ScrollView, repris de Résultats) et **dates sticky** via `stickyHeaderIndices` (groupes aplatis en enfants directs, en-têtes wrappés `bg-bg py-1.5` opaques) — pas de SectionList (contenu hétérogène, volumétrie faible). Profil : tout le bloc identité → tabs épinglé, seul le contenu d'onglet défile (le `Select` en Modal RN ne pose aucun souci dans un bloc fixe). Vérifié : suite complète (177 tests) + parcours simulateur iPhone 17 Pro (switch tabs auth immobile, scroll clavier, sticky dates poussées entre journées, 3 onglets Profil, tab bar au-dessus de l'indicateur home). Au passage : le fix clavier QWERTY du simulateur n'avait **pas** persisté — rejoué (recette du skill).
- **2026-07-10 (ter)** — **Fix : le dark mode ne s'appliquait quasiment pas en natif** (iOS + Android — seuls les icônes/chrome JS et les variants `dark:` changeaient). Cause racine : avant que react-native-css compile le CSS, la passe Lightning CSS d'`@expo/metro-config` (cibles browserslist **par défaut**, faute de config) réécrivait chaque `light-dark()` de `global.css` en « space toggle » `var(--lightningcss-light/dark)` — or react-native-css ne génère sa règle dark native (résolue via `Appearance`) que s'il reçoit un vrai nœud `light-dark()`. Fix minimal : **champ `browserslist` dans `package.json`** (plancher Chrome 123 / Safari 17.5 / Firefox 120 = support natif de `light-dark()`) ; la bascule manuelle web de `theme-preference.ts` migre du hack `--lightningcss-*` vers la propriété standard `color-scheme` sur `:root`. Au passage, vérifié que l'optimize prod de Tailwind (`@tailwindcss/node`) **exclut** le lowering LightDark → rien à changer côté postcss. Vérifié : sonde de compilation (9 → **83** règles `prefers-color-scheme` dans la stylesheet native, hex dark présents), 177 tests + typecheck/lint/format, `expo export` iOS, simulateur (dark système, bascule Clair/Sombre/Système en live, préférence « Sombre » de Corentin restaurée), web (tokens suivent `prefers-color-scheme`, forçage manuel OK, console propre). Limitation pré-existante inchangée (pas une régression) : sur web, le forçage manuel n'affecte pas les surfaces `@media (prefers-color-scheme)` (`dark:`, `.tc-shadow-*`, JS `useColorScheme`).
- **2026-07-10 (bis)** — **Fix natif : texte éclaté sur « Rejoindre une ligue »**. La phrase d'intro s'étalait sur toute la hauteur de l'écran (iOS) : react-native-css résout `line-height: var(…)` au runtime en multipliant le nombre par la taille de police, donc les `--text-*--line-height` en px de `global.css` devenaient des multiplicateurs (22px → line-height 330). Touchait tous les écrans utilisant l'échelle `text-body` (join, create, forgot-password). Fix : line-heights de l'échelle en **ratios sans unité** (mêmes px en web et natif), piège documenté dans le skill `trycast-design-system`. Vérifié au simulateur (join + create, light/dark) + `expo export` iOS + suite complète. Dans la foulée : les 3 tests rouges de `day-range.test.ts` alignés sur l'**ordre chronologique** de la bande de jours (plus ancien à gauche → plus récent à droite, décision UX de Corentin actée dans `deb4a00` ; c'étaient les tests qui étaient en retard, pas le code). Vérifié au simulateur : SAM 4 → VEN 10, aujourd'hui cerclé à droite.
- **2026-07-10** — **Outillage : pilotage du simulateur iOS par Claude**. Installation d'AXe (`brew tap cameroncooke/axe`), workflow validé de bout en bout sur iPhone 17 Pro (iOS 26.5) : boot + Metro en tâche de fond + Expo Go, screenshots lus par Claude, arbre d'accessibilité (`axe describe-ui`), taps (navigation par tabs), saisie clavier, deep links Expo Router (`exp://localhost:8081/--/<route>`, recharge le bundle). Piège corrigé : clavier AZERTY du simulateur faussait `axe type` → clavier QWERTY seul (persistant). Recette complète dans le skill `.claude/skills/trycast-ios-simulator/`.
- **2026-07-09 (bis)** — **Correctifs des premiers retours Lot 5.5** (8 retours). Cause racine des 3 plus gros : react-native-css n'enregistre pas les variables CSS au runtime natif → dark jamais appliqué, icônes tab bar sans couleur, `placeholderTextColor: var(...)` invalide (erreurs sur les écrans à champ de saisie). Fix : tokens `@theme` en littéraux `light-dark(hex, hex)`, hook `useThemeColor` + `src/tw/palette.ts` (test de synchro CSS↔TS), suppression de `useCSSVariable`. Aussi livrés : points potentiels des 3 issues 1/N/2 + barres de probabilité (module pur `potential-by-outcome`), pseudo déplacé dans Réglages (section Compte, mapper d'erreurs profil dédié), primitive `Select` (listbox ancrée), **refonte Profil** (chiffres clés, sélecteur compétition, tabs Stats/Pronos/Ligues, donut, rang via `use-my-rank` sans nouveau backend), **refonte Classement** (Select ligue, podium 2/1/3, ex æquo, barre « moi » épinglée), **bande de jours continue** sur Résultats (jours vides désactivés, aujourd'hui cerclé). Vérifié : 177 tests, `expo export` iOS OK, parcours web light+dark complet (thème dark et forçage manuel validés visuellement). **Reste : valider sur mobile réel à la J2.**
- **2026-07-09** — **Lot 5.5 livré (V0)**. Livrable Claude Design versionné dans `docs/design/` ; tokens Tailwind v4 light/dark (`light-dark()`), polices Anton/Inter, primitives `src/components/ui/` ; infra i18next (FR source, clés typées, migration `profiles.locale` + sync au login) ; passe écran par écran : auth unifiée (Connexion/Inscription segmentée), accueil Matchs maquette (mini-dashboard, compteur, auto-save), Résultats, Classement (bascule Ligues/Général), **navigation 4 onglets**, écrans ligue, Profil + **écran Réglages** (thème système/clair/sombre persisté, modale de suppression avec re-saisie du pseudo). Arbitrages Corentin : 4 onglets, auto-save, light+dark dès V0, Réglages minimal, blocs data reportés (live, distribution 1/N/2, stats par compétition, Match Detail — détail dans « blocs maquettés en attente de backend »). Vérifié web light+dark + login réel ; **reste à valider sur mobile à la J2**.
- **2026-07-07** — Cotes validées en réel : abonnement Highlightly Pro pris (1 mois), Corentin a vérifié que les runs de sync d'hier ont bien récupéré les odds ; pronos saisis pour la J2. Feuille de route : ajout du **Lot 5.5 (design system + i18n)** avant le push, stratégie i18n actée (FR source, `profiles.locale`, EN avant RWC).
- **2026-07-05** — Création du DASHBOARD. État : lots 0-5 livrés, Lot 6 (push) prochain.
