# TryCast — Dashboard

> Suivi d'avancement et décisions. Mis à jour à la fin de chaque session.
> Dernière mise à jour : **2026-07-20 (bis)** (Capture des cotes de tous les matchs futurs : fenêtre J-7 retirée dans `sync-fixtures`, budget API 20→500 — livré et déployé ; effet réel nul pour l'instant car Highlightly n'expose encore aucun match NC futur).

## Avancement des lots

| Lot | Sujet | État |
|-----|-------|------|
| 0-1 | Fondations, auth/profils, delete-account | ✅ Livré |
| 2 | Pipeline compétition (sync-fixtures, cron) | ✅ Livré |
| 3 | Prédictions + RLS deadline kickoff, pages Matchs/Résultats | ✅ Livré |
| 4 | Scoring (RPC `apply_match_scores`, EF `sync-results`, passes 1/2) | ✅ Livré, validé en réel J1 NC |
| 5 | Ligues & classements (standings, RPC, leaderboards, Realtime) | ✅ Livré |
| 5.5 | Design system + i18n (tokens light/dark, primitives, 4 onglets, i18next FR) | ✅ Livré (V0 + correctifs retours + tokens v2) |
| **6** | **Push (expo-notifications, tokens, EF notify, deep links, préférences)** | 🔶 **Déployé et testé (FCM, EF, cron, dev build Android) — reste : checklist push réels** |
| 7 | Finitions (confirmation email, changement d'e-mail, RGPD minimal, anglais + sélecteur de langue) | 🔶 **Code livré** — reste actions Corentin (dashboard Auth, deploy EF) ; OAuth + iOS/APNs **différés** |
| Web | Site vitrine Astro (`web/` : landing, waitlist beta, pages légales) | ✅ Livré et déployé sur Vercel — reste : validation des textes légaux + domaine |

## Ce qu'il reste à faire

### Lot 6 — Push : reste la checklist device
Tout est en place et déployé (tables + RPC, EF `notify` avec `verify_jwt=false`, cron `3-53/10` actif, FCM configuré, dev build Android testé, token en base). **Reste** : dérouler la checklist des push réels — rappel H-1 (pas de re-rappel au tick suivant, plus de rappel après avoir pronostiqué), push résultats après passe 1 avec les bons points (rien à la passe 2), taps → bons onglets (rappel → Matchs, résultat → Résultats). Types « Activité de ligue » et « Invitations » : v1.1+ (schéma prefs extensible). iOS/APNs au Lot 7.

### Lot 7 — Finitions : code livré (2026-07-19 bis), reste des actions Corentin
**Livré (7 commits `03350c5`→`4ab3b1e`, pas de push)** : changement d'e-mail (rangée Réglages + modale) et confirmation d'e-mail au signup (`emailRedirectTo` vers 2 pages d'atterrissage Astro `web/src/pages/app/`) ; RGPD minimal (table `consents` append-only + RLS, EF `export-data`, section Confidentialité = toggle communications + export via share sheet) ; **anglais complet** (`src/locales/en/`, 7 namespaces, test de parité) + sélecteur de langue Système/Français/English (bascule live vérifiée au simulateur) + messages de notification EN. **Différés (décision Corentin)** : OAuth Apple/Google (v2), iOS/APNs (compte Apple Developer).

**Actions restant à Corentin (bloquées par le classifieur du mode auto) :**
1. **Dashboard Supabase → Auth** : activer « Confirm email » (`enable_confirmations`, aujourd'hui `false` sur le projet hébergé — `config.toml` déjà passé à `true`) et **ajouter les URLs d'atterrissage à l'allow-list « Redirect URLs »** (`<site>/app/confirme` et `<site>/app/email-modifie`).
2. **Déployer les Edge Functions** : `supabase functions deploy export-data` (nouvelle) **et** `supabase functions deploy notify` (redéploiement pour embarquer les messages EN de `_shared/`). *(La migration `consents` est déjà `db push` + typegen, RLS vérifiée par `bash scripts/e2e-privacy.sh`.)*
3. **Vérifier `bash scripts/e2e-privacy.sh`** une fois `export-data` déployée (la section EF est ignorée tant qu'elle répond 404).
4. **SMTP** : le SMTP par défaut de Supabase est plafonné (~2 e-mails/h projet) — OK pour tester, **insuffisant pour la beta** → prévoir un SMTP custom (Resend/SES) avant ouverture. Hors périmètre code.
5. **`EXPO_PUBLIC_WEB_URL`** : ajouter au `.env` (modèle `.env.example`) l'URL du site (Vercel puis `trycast.fr`) — sert de base aux liens de confirmation. Couplé au **choix du domaine**.

### Site web / légal
- **Relire et valider les pages légales** (`web/src/pages/cgu.astro`, `confidentialite.astro`, `mentions-legales.astro`) : compléter les `[Prénom Nom]`, vérifier l'âge minimum (15 ans proposé) et l'adresse de contact `salut@trycast.fr` (l'alias existe-t-il ?), puis retirer le bandeau « premier jet » (`draft-notice` dans `web/src/layouts/content-layout.astro`). Ensuite brancher les URLs dans le footer Auth et les Réglages de l'app (requis App Store).
- **Domaine du site** : décider (trycast.fr ?) et le brancher sur le projet Vercel.

### Abonnement API
- **Highlightly Pro** (échéance ~2026-08-06) : décider du renouvellement avant chaque compétition (6N, RWC) ; sans lui, `/odds` répond 401 → fallback ×2.0, et **pas de score live in-play** (la carte LIVE en dépend).

### Affinages différés (livrés, à peaufiner en conditions réelles)
- **Carte LIVE** : projection de points chiffrés épinglés à valider avec de vraies données in-play (EF `sync-live` + cron actifs, early-exit vérifié ; s'affichera au prochain match NC en direct).

## Décisions clés (actées, ne pas re-débattre)

*Détail complet dans la mémoire projet (`trycast-project-charter`). Rappel des plus structurantes :*

- **Prono unique** par (user, match), partagé entre ligues ; points rétroactifs en rejoignant.
- Saisie = **score exact** + cases bonus offensif ; points vainqueur **pondérés par les cotes** (fallback 2.0), barème versionné en DB (`scoring_rules`).
- Deadline prono au **kickoff**, imposée par **RLS serveur**.
- **Scoring 2 temps** : passe 1 immédiate sans bonus offensif, passe 2 après saisie admin des essais (aucun fournisseur ne les expose — définitif MVP).
- Logique de scoring en **module TS pur** + écriture atomique via **une seule RPC** `apply_match_scores`.
- Source de données : **Highlightly** (API-Sports, Sportradar, etc. écartés).
- **4 onglets** (Lot 5.5, remplace les 5 du Lot 5) : Matchs / Résultats / Classement / Profil — les ligues sont ventilées (CTAs sur l'accueil, bascule Ligues/Général dans Classement, gestion via le détail de ligue).
- **Auto-save optimiste du prono** (Lot 5.5, remplace le bouton Valider) : statut À pronostiquer → Enregistrement… → Enregistré dans la carte.
- Ligues : création/adhésion **uniquement par RPC** (code d'invitation jamais requêtable) ; owner supprime son compte → sa ligue disparaît en cascade (transfert de propriété livré au 2026-07-16) ; l'owner ne quitte pas sa ligue, il la supprime.
- **i18n : FR langue source via i18next** (clés typées, namespaces par domaine), `profiles.locale` synchronisée au démarrage pour les notifications du Lot 6 ; EN livré au plus tard avant la RWC 2027.
- **Design system** : identité vert bouteille/crème/grenat (« le grenat est une étincelle, jamais un fond »), Anton + Inter, light + dark complets. Référence : `docs/design/` + skill `trycast-design-system`.

## Points ouverts / dette assumée
- ~~App lit encore `BAREME_V1` hardcodé pour l'aperçu~~ → **résolu (2026-07-20)** : hook `useActiveScoringRules` lit le barème actif depuis `scoring_rules` ; `BAREME_V2` n'est plus que fallback/`initialData` + ancre du test de parité.
- **Chantier reporté (cotes)** : capturer les cotes plus tôt / plus fiablement (élargir `selectMatchesForOddsCapture`, ne jamais écraser une bonne cote par du vide) pour réduire le fallback ×2.0 — à traiter dans une prochaine feature.
- Override npm `react-native-css` → `lightningcss@1.30.1` (bug bundling natif) — à réévaluer quand react-native-css > 3.0.7 sort.
- **Contrainte tokens natifs** (découverte 2026-07-09) : react-native-css n'enregistre pas les variables `:root`/`@theme` au runtime → les `--color-*` de `global.css` doivent rester des littéraux ou `light-dark(hex, hex)` sans `var()` interne ; hex dupliqués dans `src/tw/palette.ts` (couleurs lues en JS via `useThemeColor`), synchronisation verrouillée par `src/tw/palette.test.ts`.
- Barre « moi » épinglée du Classement : le rang du joueur au-dessus est approximé à rang − 1 (exact hors ex æquo exotiques, documenté dans `pinned-me-row.tsx`).
- **Plancher browserslist obligatoire** (fix 2026-07-10) : le champ `browserslist` de `package.json` (Chrome 123 / Safari 17.5 / Firefox 120 minimum) garantit que `light-dark()` survit à la passe Lightning CSS d'`@expo/metro-config` — le retirer ou le baisser re-casse silencieusement tout le dark natif. La bascule manuelle web passe par `color-scheme` sur `:root` (`theme-preference.ts`).
- Tailwind : la palette par défaut (`bg-blue-600`…) reste techniquement disponible — la neutraliser (`--color-*: initial`) maintenant que tous les écrans sont sur les tokens.
- Identifiants encore hardcodés (signalés) : `package.json` (typegen `--project-id`), `app.json` (owner EAS, requis), migration cron `20260705000300` (URL projet).
- Base dev : 6 matchs J1 NC ont `scored_at` mais 0 prono scoré → classement général NC vide (état donnée, pas un bug).

## Journal des sessions
- **2026-07-20 (bis)** — **Capture des cotes de tous les matchs futurs** (1 commit `760821d`, pas de push). **Demande Corentin** : arrêter de ne capturer les cotes que des matchs à J-7 ou moins, les prendre pour **tous les matchs futurs même lointains** (cas NC étalée sur des mois). **Constat de planif tranché avec Corentin** : la récupération des *matchs* n'a **jamais eu** de filtre de date (`fetchAllMatches` pagine toute la saison) — seule la *capture des cotes* était bridée (`selectMatchesForOddsCapture`, fenêtre `ODDS_CAPTURE_WINDOW_MS = 7j`). Donc **rien à changer côté fixtures**. Livré : borne haute retirée (on garde `scheduled` + `kickoff > now`, tri **kickoff croissant** pour servir les prochains matchs d'abord si le budget sature) ; **`MAX_API_CALLS_PER_RUN` 20→500** — indispensable car le budget est partagé sur le run et la boucle est par compétition : sinon les cotes d'une compétition affameraient le `fetchAllMatches` des suivantes (reste ≪ 7500/j du plan Highlightly Pro). Vérifié : typecheck/lint/format + **272 tests** (cas `selectMatchesForOddsCapture` réécrits : retient les futurs lointains triés, écarte passés/non-scheduled). Déployé (`supabase functions deploy sync-fixtures`) + **run manuel live** via `net.http_post` + secret Vault : `success`, 1 appel API, **0 cote capturée — attendu**. **Découverte importante (lié au point #2)** : l'API Highlightly ne renvoie **que 18 matchs NC 2026, tous `finished` (4-18 juillet), aucun futur** — les journées suivantes ne sont pas encore publiées côté fournisseur. Le code est prêt : dès que des fixtures futures apparaîtront, elles seront récupérées et leurs cotes capturées **sans limite de distance**. Corentin : « c'est normal, Highlightly publie au fil de l'eau » → pas d'investigation saison/leagueId pour l'instant.
- **2026-07-20** — **Barème de scoring v2** (3 commits `6eff11c`→`4e21c99`, pas de push). **Décisions Corentin** : socle vainqueur `winnerPointsPerOddsUnit` 10→**15** ; bonus offensif indexé sur la **cote de victoire de l'équipe** (`0.25 × 15 × odds[side]`) au lieu de celle du vainqueur → une attaque d'outsider paie plus ; nouveau **malus −10** par case offensive cochée mais ratée (couplé au bon vainqueur, en passe 2 seulement), **total plafonné à 0** ; bonus fixes (exact 50, écart 15/8, défensif 5) inchangés. Le calcul reste **mono-source TS** (`computeMatchPoints`) : clamp + malus en TS, RPC `apply_match_scores` inchangée. **App branchée sur la DB** : hook `useActiveScoringRules` lit `scoring_rules` (fin de la constante en dur ; `BAREME_V2` = fallback/ancre parité). **Breakdown enrichi** (`offensiveHome`/`offensiveAway` : cote, essais, points ; `offensiveBonusPending` gardé à la racine pour le SELECT passe 2). **Sheet de détail** refondu (lignes offensives par équipe, **malus en rouge** via ton `danger` ajouté au `Badge`, **badge bonus défensif** sur le sheet + la result-card, volets « en attente ») ; **prediction-card** : aperçu bonus par cote d'équipe + risque `−10`, indicateur bonus défensif quand l'écart pronostiqué qualifie. **Backfill** : `sync-results` gagne une **sélection D** (re-scoring auto des pronos `scoring_rule_version ≠` version active, auto-extincteur, borné aux compétitions actives). Rollout via MCP : migration `20260720000100` (v2 active, v1 désactivée) `db push` + typegen (aucun diff, jsonb), EF `sync-results` redéployée, **10 matchs de `nc-2026` re-scorés en v2** (`restant_v1 = 0`). Vérifié : typecheck/lint/format + **272 tests** (dont parité barème TS↔DB et parité i18n EN/FR), **E2E serveur** (`e2e-scoring.sql` : passe 1 `(31,1,0)`, passe 2 `(37,1,0)`, -104 `(105,2,1)`, idempotence, extinction pending) + **E2E client** (`e2e-scoring.sh` : barème v2 inviolable, RPC verrouillée). **Reste (optionnel)** : vérif visuelle simulateur iOS (sheet/badges/aperçu), et le chantier « capture des cotes plus tôt » reporté. Piège test : le standings étant un agrégat absolu par compétition, un prono scoré résiduel (-102) faussait l'E2E → seed durci (reset de l'état scoré de toute la compétition).
- **2026-07-19 (bis)** — **Lot 7 (finitions release) : code livré en 7 commits** (`03350c5`→`4ab3b1e`, pas de push). Périmètre arbitré avec Corentin : **e-mail** (changement d'adresse + réactivation confirmation, atterrissage sur 2 pages statiques du site `web/` plutôt que du deep-linking natif), **RGPD minimal** (table `consents` append-only + RLS own-rows, EF `export-data` sur le modèle `delete-account`, export via share sheet natif — pas d'e-mail, aucune brique SMTP), **anglais complet** (7 namespaces `src/locales/en/` + test de parité récursif FR/EN car le typage ne couvre que FR + messages de notification EN) et **sélecteur de langue** Système/Français/English (persisté, poussé dans `profiles.locale`). Contrainte respectée : **zéro nouvelle lib native, aucun rebuild** (Share du cœur RN, expo-clipboard/file-system déjà là). Vérifié : typecheck/lint/format + **271 tests**, `e2e-privacy.sh` (RLS consents live), pages web (light+dark), **simulateur iOS** (Réglages : rangée e-mail, section Confidentialité, bascule de langue FR↔EN en direct). Migration `consents` déjà `db push` + typegen. **Déploiement des EF `export-data` et `notify` + réglages Auth = actions Corentin** (classifieur), voir « reste à faire ». **Piège vécu** : `cd web` détourné par zoxide vers un autre projet (cernix) → toujours chemins absolus/`builtin cd` (mémoire `zoxide-cd-hijack`).
- **2026-07-18 (bis)** — **Overlay de célébration des pronos gagnés** (nouveau domaine `src/features/celebration/`). À la première connexion après qu'un ou plusieurs pronos ont rapporté des points, un moment de récompense plein écran avec compteur animé et récap multi-matchs. **Décisions actées** : overlay plein écran (pas une sheet), périmètre **gagnés uniquement** (`points_awarded > 0`), persistance **locale device** (AsyncStorage, pas de migration), animation **sans nouvelle lib** (Reanimated + expo-haptics → aucun rebuild). Tout se calcule **client** depuis le cache existant (`useMyPredictions`/`useMatches`/`useActiveCompetition`), zéro réseau ni colonne DB. Livré : `celebrated-matches-store.ts` (**anti-rétroactif** : premier lancement absorbe l'historique sans rien afficher), `build-celebration-items.ts` (sélection pure testée), `use-celebration.ts` (évaluation une fois par montage), `celebration-overlay.tsx` (Modal, total `font-display` grenat, haptic Success, `useReducedMotion`), `animated-points-counter.tsx`, namespace i18n `celebration`, montage `CelebrationHost`. Vérifié : typecheck/lint/format + **264 tests**, simulateur (baseline puis overlay « 141 pts · 9 pronos gagnés »). 1 commit (`3290bc6`). Pas de push.
- **2026-07-17 (quater)** — **Splash screen + icône d'app aux couleurs TryCast** (le splash/l'icône par défaut étaient ceux du template Expo, bleu). **Décision Corentin : fond d'icône = vert marque #14432a** (ballon grenat + traînée). Livré : splash natif (`splash-icon.png` régénéré, fond `bg` light+dark), icônes (iOS `icon.png` 1024, adaptive Android fond `#14432a`, favicon), overlay JS réécrit pour afficher le même splash sur fond sable (bascule invisible), nettoyage des assets template. Vérifié : typecheck/lint/format + 251 tests, smoke test simulateur. 3 commits (`48d5a2c`→`468a4ee`). Pas de push.
- **2026-07-17 (ter)** — **Cross-fade au changement de thème : essayé puis abandonné** (décision Corentin). Implémentation complète livrée et vérifiée, mais la **latence ajoutée avant la bascule** (~0,3-0,5 s : capture PNG + affichage overlay) n'a pas plu → **reset à `562f162`**, la bascule reste instantanée. Voir mémoire `theme-crossfade-abandonne`. Aucun commit conservé côté app.
- **2026-07-17 (bis)** — **Drapeaux nationaux dans le ballon-comète (`TeamFlag`)** : remplacer le tricode par le drapeau du pays dans la forme de l'icône, en SVG. **Décisions Corentin** : drapeaux simplifiés dessinés main (zéro dépendance), coutures sans tricode quand le drapeau existe, couverture = les 12 nations du Nations Championship (repli ballon uni + tricode sinon). Livré : `team-flags.ts` (defs déclaratives par tricode + tests) et `team-flag.tsx` (`ClipPath` ellipse rotée, `useId()`). Vérifié : typecheck/lint/format + **251 tests**, simulateur (12 drapeaux sur cartes réelles, dark + light). 2 commits (`24d5e08`, `12c61f9`). Pas de push.
- **2026-07-17** — **Retour haptique (expo-haptics)**, pour profiter du rebuild Android déjà dû à expo-clipboard. **Périmètre validé** : haptic succès à l'enregistrement d'un prono complet + tick léger à la copie du code d'invitation — pas de pull-to-refresh, pas de haptic bonus/boutons (« le vibreur reste une étincelle »). Livré : helper `src/lib/haptics.ts` (erreurs avalées : un haptic ne casse jamais l'UI, l'ancien build sans module natif ne crashe pas), branchements. Vérifié : typecheck/lint/format + **246 tests**. 2 commits (`7d4e15c`, `9a11305`). Pas de push.
- **2026-07-16** — **Les 2 dernières pages MVP : détail de ligue (3 onglets) + Créer/Rejoindre fusionné.** **Décisions Corentin** : transfert de propriété implémenté (revient sur « transfert = v2 »), plafond 50 membres, expo-clipboard accepté. **Backend (4 migrations `20260716…`)** : `leagues.color`, plafond 50 dans `join_league`, `preview_league`, `transfer_league_ownership`, `get_league_round_points`. `e2e-leagues.sh` étendu, tout vert. **Feature** : `colors.ts`, `initialsOf`, `groupRoundPoints` + tests, hooks preview/transfert/round-points, `extractInviteCode`. **Composants** : LeagueIcon, LeagueColorPicker, InviteCodeInput, LeaguePreviewSheet, RoundStrip, 4 modales DS. **Écrans** : `league/new` (segmented Créer/Rejoindre), `league/[id]` refondu. Vérifié : typecheck/lint/format + **246 tests**, simulateur light et dark. **3 pièges documentés dans les skills** : `border-dashed` propagé aux descendants, `instanceof PostgrestError` KO sous Hermes (lecture duck-typée du `code`), quitter/supprimer sans pile de nav (`router.canGoBack()`). 7 commits (`70c662b`→`727ae29`). Pas de push.
- **2026-07-15 (ter)** — Push par Corentin : **CI web verte et site déployé sur Vercel, confirmé fonctionnel**. Restent côté Corentin : validation des textes légaux et choix du domaine.
- **2026-07-15 (bis)** — **Fix CI/Vercel du site web : découverte tsconfig de rolldown.** Symptôme `Tsconfig not found expo/tsconfig.base` en CI web et sur Vercel (build local OK) : la découverte tsconfig de vite/rolldown escalade au-dessus de `web/` et parse le tsconfig racine (app Expo). Correctifs : `resolve.tsconfigPaths: false`, script waitlist sorti en `web/src/scripts/waitlist.js`, stub `node_modules/expo/tsconfig.base.json = {}` en CI + `installCommand` de `web/vercel.json`. Piège documenté dans le skill `trycast-site-web`.
- **2026-07-15** — **Site vitrine Astro (`web/`)** : même repo, sous-dossier autonome (pas de workspaces npm), hébergement Vercel, pages légales en premiers jets. **Livré** : scaffold Astro 7 statique (tokens DS copiés en CSS vanilla, Anton/Inter self-hostées, thème light/dark), landing recréée depuis la maquette, pages légales CGU/confidentialité/mentions, **waitlist Supabase** (migration `20260715000100`, RPC `join_waitlist` security definer anon avec anti-spam SQL + honeypot, E2E validé puis purgé). **CI scindée** (`web.yml` / `ci.yml` ignore `web/**`). Piège : `on conflict (email)` ambigu avec le paramètre RPC → cibler la contrainte par son nom. Piège preview : après scroll, translater `document.body` en JS (capture sinon vide).
- **2026-07-14 (bis)** — **Audit natif des écrans + unification clavier/headers** (lots A/B/C validés, lot D reporté). **Lot A (clavier)** : primitive `Screen` enrichie (pattern clavier auth intégré, variantes top/bottom), adoptée partout où l'écran est un simple scroll ; **la tab bar flottante s'escamote quand le clavier est ouvert** (critique sur Android). **Lot B (headers natifs)** : thème navigation construit depuis `palette.ts` (`src/tw/navigation-theme.ts`), `screenOptions` du stack (app), Réglages passe sous header natif. **Lot C** : fond blanc en dur du stack auth supprimé (flash dark), sheet des points calée sur la barre gestuelle, expo-glass-effect désinstallé. **Décisions actées** : tab bar custom conservée, pas d'expo-blur, `Screen` = porteur unique du pattern clavier. Vérifié : typecheck/lint/format + **224 tests**, simulateur light et dark. **Piège documenté** : clavier logiciel absent au focus = « Connect Hardware Keyboard » du simulateur (`defaults write com.apple.iphonesimulator ConnectHardwareKeyboard -bool false` + redémarrage). **Reste à valider sur l'Android réel** : tab bar au clavier + resize des écrans à inputs. 12 commits. Pas de push.
- **2026-07-13 (quinquies)** — **ResultCard à deux zones tactiles** (maquette mise à jour, commit `b4dfa34`) : zone A (heure/verdict + équipes/score) → page de détail du match ; zone B (ton prono + points + « Voir le détail des points ») → sheet du barème. `result-card.tsx` réécrit (deux `Pressable`, `will-change-variable` dans les classes de base), prop `onOpenMatch` optionnelle (fournie par Résultats et Profil > Pronos, absente sur le détail lui-même). Sheet du barème + section « Bonus pronostiqués ». Note : l'export design `b4dfa34` a **remis la timeline** dans la maquette Match Detail (assumé, le design sera remis à jour plus tard) — la timeline reste hors MVP, ne pas la porter.
- **2026-07-13 (quater)** — **Page de détail de match livrée (dernier bloc maquetté), timeline retirée du MVP** (aucune source de données d'événements, spike Highlightly). Header natif, **entrée = carte LIVE uniquement**, pronos des autres jamais visibles avant kickoff. Backend : migration `20260713000100` — RPC `get_match_league_predictions(match, league)` security definer (garde `is_league_member` anti-énumération, **masquage serveur `now() >= kickoff_at`**, left join = membres sans prono). App : hooks `useMatch(id)` (refetch 60s seulement si `in_play`) et `useMatchLeaguePredictions`, module pur `match-phase.ts`, composants `match-hero`/`match-status-chip`/`locked-prediction-card`/`masked-predictions`/`member-prediction-row`, écran `match/[id].tsx` (3 phases + segmented Pronos/Classement). Vérifié : typecheck/lint/format + **224 tests**, e2e verts, simulateur 3 états × light/dark. **Fausse alerte classée** : l'engrenage flottant des screenshots est le **FAB natif d'expo-dev-menu**, pas le bouton Réglages (documenté au skill simulateur). 9 commits (`328cde5`→`58ea834`). Pas de push.
- **2026-07-13 (ter)** — **Animations « magic pill » (Reanimated 4)** : (1) tab bar — la pastille active devient un élément UNIQUE qui coulisse (ressort amorti), icône+label en cross-fade d'opacité (géométrie mesurée par `onLayout`) ; (2) contrôle segmenté (primitive partagée) — même principe glissé smooth sans rebond. **Décision technique** : cross-fade d'opacité plutôt qu'`interpolateColor` (`useThemeColor` renvoie une `var()` CSS non interpolable sur web). Anti-flash au 1er rendu, `useReducedMotion`. Aucune dépendance ni rebuild. Vérifié : typecheck/lint/format + **220 tests**, simulateur light et dark. 2 commits (`88a123d`, `3193351`). Pas de push.
- **2026-07-13** — **3 blocs maquettés en attente de backend traités** (mot de passe, photo de profil, carte LIVE). **(1) Mot de passe** — pur client `auth.updateUser`, modale + rangée Réglages, réutilise `validatePassword`/`toAuthMessageKey`. **(2) Photo de profil** — migrations `20260712000100`→`000400` (colonne `avatar_url` + grant colonne, bucket `avatars`, policies insert/update/delete/**select** « son dossier »), upload/compression client (`expo-image-picker`/`-manipulator` 512px), purge dossier dans EF `delete-account`, `e2e-avatars.sh`. **Piège débogué** : upsert **et** remove Storage font un SELECT d'existence sous RLS → policy SELECT « son dossier » indispensable (sinon 403 malgré écriture correcte). **(3) Carte LIVE (prépa code)** — colonnes `live_*`, EF `sync-live` (transform pur testé), `live-match-card.tsx` + `use-live-matches`. **Découverte** : le rebuild doit être un prebuild `--clean` sinon `NSPhotoLibraryUsageDescription` manque (crash TCC). Carte LIVE ensuite activée par Corentin (secret + Vault + deploy + cron + clé Pro). Vérifié : typecheck/lint/format + **220 tests**, `e2e-avatars.sh` OK, simulateur. 9 commits (`f859a0c`→`8ef187d`). Pas de push.
- **2026-07-12 (bis)** — **Tokens couleurs DS v2 : surfaces neutres (charbon/sable).** Corentin a mis à jour le DS sur Claude Design ; répercussion dans l'app. Les **surfaces** passent de vert/crème à des **neutres chauds** (sable/blanc en light, charbon chaud en dark) ; le **vert devient la marque** (`brand`, avatars/icônes), le grenat reste l'étincelle. Re-thème automatique via les tokens sémantiques : 3 fichiers app touchés (`global.css`, `palette.ts`, `app-tabs.tsx`). Vérifié : typecheck/lint/format + **212 tests**, simulateur light et dark. 2 commits (`070a6cd`, `34a8322`). Pas de push.
- **2026-07-12** — **Config iOS locale (dev build) — parité avec Android.** Le lot Android avait ajouté `expo-dev-client` : `expo start --ios` n'ouvrait plus Expo Go mais attendait un dev build. Au blame : aucune config iOS n'avait jamais été retirée (Android avait *gagné* de la config). Choix acté : **dev build local iOS** (pas Expo Go). Livré : `app.json` (`ios.bundleIdentifier = com.cohozen.trycast`, `supportsTablet: false`), scripts `expo run:ios`/`run:android`, profil EAS `development` iOS `simulator: true`, skill simulateur mis à jour. Prérequis : `brew install cocoapods`. Vérifié : typecheck/lint/format + **212 tests**.
- **2026-07-11 (quater)** — **Blocs maquettés débloqués** : courbe « Points par journée » (module pur `compute-points-by-round.ts`, 100 % client, données déjà en cache) + distribution communautaire 1/N/2 (migration `20260711000400`, RPC `get_prediction_distributions` security definer, **visible pendant toute la période de pronos**). **Décision Corentin** : GO du spike score live (la clé Highlightly **Pro** renvoie `state.score` avec 3-5 min de décalage ; le null de juillet venait de la clé FREE). Vérifié : **212 tests** (+8), typecheck/lint/format, `e2e-predictions.sh` 10/10, simulateur light et dark.
- **2026-07-11 (ter)** — **Mise en route du Lot 6 par Corentin + diagnostic du cron muet.** Corentin a tout déroulé (clé FCM, env EAS, EF `notify` déployée, cron actif, premier build EAS Android validé). Les ticks notify prenaient un **401 « Missing authorization header »** : l'EF était déployée avec `verify_jwt=true` car `config.toml` ne déclarait pas `[functions.notify]`. Fix : bloc `verify_jwt=false` ajouté, redeploy fait par Corentin. README : section development build.
- **2026-07-11 (bis)** — **Lot 6 livré (code) : notifications push, Android d'abord** (13 commits). Firebase `trycast-90446` créé par Corentin, package `com.cohozen.trycast` ; iOS/APNs au Lot 7. Backend : `push_tokens` (écriture par RPC security definer — l'upsert PostgREST ne peut pas réaffecter la ligne d'un autre compte sous RLS), `notification_prefs`, `notification_sends` (dédup **claim-first** sur unique (user, match, type)), RPC cibles rappel/résultat, **une seule EF `notify`** scannante (receipts → rappels H-1 → résultats passe 1) sur cron `3-53/10`. App : `expo-notifications`, feature `src/features/notifications/`, deep links au tap via **allowlist** `data.url`, section Notifications des Réglages. Vérifié : **204 tests**, `e2e-notifications.sh` 11/11, `expo export` iOS, simulateur.
- **2026-07-11** — **Troisième série de retours Lot 5.5** (6 retours) : la bande de jours présélectionne le jour avec matchs le plus proche d'aujourd'hui, la modale de détail des points affiche « Points de base · 1 N 2 », la précision du Profil intègre les **non-pronostiqués** au dénominateur (catégorie dédiée, segment neutre dans le donut), l'onglet Pronos liste tous les matchs terminés. Vérifié : 180 tests + simulateur light et dark.
- **2026-07-10 (sexies)** — **Fix warning react-native-css « added or removed a variable » au press des boutons** : les classes de press (`scale-[0.97]`, `shadow-none`…) compilent en variables CSS, et react-native-css démonte/remonte le composant quand le statut « porteur de variables » bascule. Fix : classe `will-change-variable` dans les classes de base du `Pressable` des primitives `button`/`icon-button`/`chip`. Documenté dans le skill DS. Vérifié : 177 tests + simulateur.
- **2026-07-10 (quinquies)** — **Safe areas généralisées** : hook `useScreenInsets()` (`top = max(inset, 56)`, `bottomTabBar = 94 + max(inset, 16)`, planchers = anciennes valeurs fixes → rendu identique + anti-chevauchement Android). **Piège react-native-css** : `contentContainerStyle` inline écrasait tout le `contentContainerClassName` → le wrapper `ScrollView` de `@/tw` fusionne classes résolues + inline. Documenté dans le skill DS. Vérifié : 177 tests + simulateur (Android à valider sur appareil réel).
- **2026-07-10 (quater)** — **Deuxième série de retours Lot 5.5** (6 retours ergonomie) : auth alignée en haut + champ actif maintenu visible au clavier (`automaticallyAdjustKeyboardInsets`), tab bar flottante décollée de la barre système (`useSafeAreaInsets`, plancher 16px), « Mes matchs » en-tête épinglé + **dates sticky** (`stickyHeaderIndices`), Profil identité/tabs épinglé. Vérifié : 177 tests + simulateur.
- **2026-07-10 (ter)** — **Fix : le dark mode ne s'appliquait quasiment pas en natif.** Cause racine : la passe Lightning CSS d'`@expo/metro-config` (cibles browserslist par défaut) réécrivait chaque `light-dark()` en « space toggle » `var(--lightningcss-*)`, que react-native-css ne sait pas résoudre. Fix minimal : **champ `browserslist` dans `package.json`** (plancher Chrome 123 / Safari 17.5 / Firefox 120) ; bascule web migrée vers `color-scheme` sur `:root`. Vérifié : sonde de compilation (9 → **83** règles `prefers-color-scheme`), 177 tests + typecheck/lint/format, simulateur + web.
- **2026-07-10 (bis)** — **Fix natif : texte éclaté sur « Rejoindre une ligue »** : react-native-css résout `line-height: var(…)` au runtime en le multipliant par la taille de police → les `--text-*--line-height` en px devenaient des multiplicateurs. Fix : line-heights de l'échelle en **ratios sans unité**. Dans la foulée, les 3 tests rouges de `day-range.test.ts` alignés sur l'ordre chronologique de la bande de jours (c'étaient les tests qui étaient en retard, décision UX de Corentin `deb4a00`). Vérifié au simulateur + `expo export` iOS.
- **2026-07-10** — **Outillage : pilotage du simulateur iOS par Claude.** Installation d'AXe (`brew tap cameroncooke/axe`), workflow validé de bout en bout sur iPhone 17 Pro : screenshots lus par Claude, arbre d'accessibilité (`axe describe-ui`), taps, deep links Expo Router. Piège corrigé : clavier AZERTY du simulateur faussait `axe type` → clavier QWERTY seul. Recette dans le skill `trycast-ios-simulator`.
- **2026-07-09 (bis)** — **Correctifs des premiers retours Lot 5.5** (8 retours). Cause racine des 3 plus gros : react-native-css n'enregistre pas les variables CSS au runtime natif → tokens `@theme` passés en littéraux `light-dark(hex, hex)`, hook `useThemeColor` + `src/tw/palette.ts` (test de synchro CSS↔TS), suppression de `useCSSVariable`. Aussi : primitive `Select`, **refonte Profil** (chiffres clés, tabs, donut, rang) et **Classement** (podium, ex æquo, barre « moi » épinglée), bande de jours continue sur Résultats. Vérifié : 177 tests, `expo export` iOS, web light+dark.
- **2026-07-09** — **Lot 5.5 livré (V0).** Livrable Claude Design versionné dans `docs/design/` ; tokens Tailwind v4 light/dark, polices Anton/Inter, primitives `src/components/ui/` ; infra i18next (FR source, clés typées, migration `profiles.locale` + sync au login). Passe écran par écran : auth unifiée, accueil Matchs, Résultats, Classement, **navigation 4 onglets**, écrans ligue, Profil + **Réglages**. Arbitrages Corentin : 4 onglets, auto-save, light+dark dès V0, Réglages minimal, blocs data reportés.
- **2026-07-07** — Cotes validées en réel : abonnement Highlightly Pro pris (1 mois), sync des odds vérifiée, pronos J2 saisis. Feuille de route : ajout du **Lot 5.5 (design system + i18n)** avant le push, stratégie i18n actée (FR source, `profiles.locale`, EN avant RWC).
- **2026-07-05** — Création du DASHBOARD. État : lots 0-5 livrés, Lot 6 (push) prochain.
