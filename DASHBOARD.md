# TryCast — Dashboard

> Suivi d'avancement et décisions. Mis à jour à la fin de chaque session.
> Dernière mise à jour : **2026-07-14 (bis)** (audit natif : clavier unifié via Screen, headers natifs thémés DS, Réglages sous header natif)

## Avancement des lots

| Lot | Sujet | État |
|-----|-------|------|
| 0-1 | Fondations, auth/profils, delete-account | ✅ Livré |
| 2 | Pipeline compétition (sync-fixtures, cron) | ✅ Livré |
| 3 | Prédictions + RLS deadline kickoff, pages Matchs/Résultats | ✅ Livré |
| 4 | Scoring (RPC `apply_match_scores`, EF `sync-results`, passes 1/2) | ✅ Livré, validé en réel J1 NC |
| 5 | Ligues & classements (standings, RPC, leaderboards, Realtime) | ✅ Livré |
| 5.5 | Design system + i18n (tokens light/dark, primitives, 4 onglets, i18next FR) | ✅ **Livré (V0 + correctifs retours + tokens v2)** |
| **6** | **Push (expo-notifications, tokens, EF notify, deep links, préférences)** | 🔶 **Déployé (FCM, EF, cron, dev build testé) — reste : redeploy `notify` (fix 401) + checklist push réels** |
| 7 | Finitions (confirmation email, EN si beta anglophone, pages légales, etc.) | ⬜ À venir |

## Ce qu'il reste à faire

### Lot 6 — Push : tout est en place sauf un redeploy
Livré et mis en route le 2026-07-11 : tables + RPC en dev, EF `notify` déployée, cron `3-53/10` actif, FCM configuré (clé de service uploadée aux credentials EAS, `GOOGLE_SERVICES_JSON` en env EAS), **premier dev build Android validé par Corentin** (APK installé, token enregistré en base). **Android d'abord** ; iOS/APNs au Lot 7. Types « Activité de ligue » et « Invitations » : ni toggle ni backend (v1.1+, schéma prefs extensible). **Reste** : le redeploy `notify` (fix `verify_jwt`, détail dans « En attente ») puis la fin de la checklist device (rappel H-1 réel, push résultats, taps → bons onglets).

### Écrans : blocs maquettés en attente de backend (reportés du Lot 5.5)

*Les maquettes de `docs/design/` vont plus loin que ce que les données permettent aujourd'hui. Ce qui suit est **prêt côté design** (et souvent côté clés i18n) mais bloqué par du serveur/data à construire — chaque bloc liste son préalable.*

**Accueil Matchs** (`MesMatchs.dc.html`)
- ~~**Carte LIVE**~~ → **livré ET activé le 2026-07-13.** Colonnes `live_*` sur `matches` (migration `20260712000500`, distinctes de `home_score`), EF `sync-live` (fenêtre kickoff→+3h, lit `in_play`, écrit seulement `live_*`) déployée + secret Vault `sync_live_secret` + cron `sync-live-5min` (actif) ; clé Highlightly **Pro** en place. Carte `live-match-card.tsx` en tête de l'accueil (badge En direct, période via `state.description`, différé assumé, rappel du prono), hook `use-live-matches` (polling 60s). **Vérifié** : tick manuel via secret Vault → `200 {"skipped":true}` (EF OK, secret OK, early-exit hors match). Se déclenchera au prochain match NC en cours. La minute exacte n'est pas exposée → fallback période. **Reste (affinage à l'activation réelle)** : projection de points chiffrés épinglés, à valider avec de vraies données in-play.
- ~~**Barres de distribution des pronos 1/N/2**~~ → **livré le 2026-07-11.** RPC `get_prediction_distributions(competition)` `security definer` (agrégats 1/N/2, jamais de prono individuel). **Décision Corentin : visible pendant toute la période de pronos, y compris avant kickoff** (assumé pour donner du peps ; caveat petit effectif). Sur la carte prono, les barres communautaires **remplacent** les barres de cotes dès qu'au moins un prono existe (repli cotes sinon) ; bloc « La communauté a joué » aussi sur ResultCard (Résultats + Profil > Pronos). Chargé une fois au montage (pas de temps réel).

**Résultats / Match Detail** (`TryCast Match Detail.dc.html`)
- ~~**Écran entier**~~ → **livré le 2026-07-13** (route `match/[id]`, RPC `get_match_league_predictions`, détail au journal). **La timeline est retirée du MVP** (décision Corentin 2026-07-13) : données événements (essais/cartons minute par minute) **inexistantes chez tous les providers testés** (spike clos, `docs/spike-highlightly.md`) — elle reste visible dans la maquette (revenue avec l'export design `b4dfa34`, assumé : le design sera remis à jour plus tard), ne pas la porter côté app. Points d'entrée : **carte LIVE** (accueil) + **ResultCard à deux zones** (Résultats et Profil > Pronos, livré le 2026-07-14) ; pas de navigation depuis les cartes de prono de l'accueil (décision).

**Profil** (`TryCast Profil.dc.html`) — refondu le 2026-07-09 (header + chiffres clés, sélecteur de compétition, tabs Stats/Pronos/Ligues, donut de précision, rang au général). Reste bloqué par le backend :
- ~~**Courbe « Points par journée »**~~ → **livré le 2026-07-11, sans backend.** Module pur `compute-points-by-round.ts` (agrège `points_awarded` par `round`, tri par min(kickoff), rounds sans match terminé exclus, cumul) — les données étaient déjà en cache client (`useMatches` + `useMyPredictions`). Chart SVG `points-trend-chart.tsx` + badge des points de la dernière journée ; état vide conservé si aucune journée entamée.
- ~~**Photo de profil** (Changer/Supprimer, maquette Réglages)~~ → **livré le 2026-07-13.** Bucket Storage `avatars` (public read) + colonne `profiles.avatar_url` (grant par colonne) + policies « son propre dossier » insert/update/delete/**select** (migrations `20260712000100`→`000400`). **Piège vérifié** : l'upsert ET le remove passent par un SELECT d'existence sous RLS → sans policy SELECT, 403 « new row violates RLS » alors que les policies écriture sont bonnes. Upload/compression 100 % client (`expo-image-picker` + `expo-image-manipulator`, 512px JPEG, cache-buster `?v=`), `Avatar` affiche la photo (repli initiales) sur profil/podium/classement (RPC leaderboards → `avatar_url` ajouté au retour), purge du dossier dans l'EF `delete-account`. `scripts/e2e-avatars.sh` OK. **Modules natifs → nécessite un rebuild du dev client** avant test visuel.

**Réglages** (`TryCast Reglages.dc.html` — livré en V0 minimal : thème, langue affichée, version, déconnexion, zone danger)
- ~~**Notifications** (master + par type + bannière « bloquées dans iOS »)~~ → **livré au Lot 6** (2 types câblés ; « Activité de ligue » et « Invitations » suivront leur backend).
- ~~**Changement de mot de passe** (sheet maquettée avec erreurs)~~ → **livré le 2026-07-13.** Hook `use-update-password` (`supabase.auth.updateUser`, pur client, pas de confirmation e-mail), modale `PasswordEditorModal` (validation locale + erreurs serveur `same_password`/`weak_password` via `toAuthMessageKey` réutilisé du domaine auth), rangée dans la section Compte des Réglages.
- **Changement d'e-mail** (ligne « Modifier ») → `supabase.auth.updateUser` **+ e-mails de confirmation** ; reste couplé à la réactivation de la confirmation email (Lot 7).
- **RGPD** : sheet consentements (mesure d'audience, communications, avec dates de recueil) + « Exporter mes données » → table `consents` + EF d'export (envoi par e-mail) ; n'a de sens qu'une fois un analytics en place.
- **Liens légaux** (CGU, politique de confidentialité, mentions légales) → pages à rédiger et héberger (requis App Store — Lot 7). Le footer légal de la maquette Auth attend les mêmes pages.
- **Sélecteur de langue actif** (aujourd'hui « Français » en lecture seule) → livraison de l'EN (infra prête, traduction avant RWC 2027).
- **Compte social-only** (« Géré par Apple », pas de mot de passe) → OAuth Apple/Google (v2), comme les boutons « Continuer avec Apple/Google » de la maquette Auth.

### En attente côté Corentin (actions manuelles)
- [x] **`supabase functions deploy notify`** (fix `verify_jwt=false`) — **fait** par Corentin le 2026-07-11 (soir). Trop tard toutefois pour le rappel H-1 du dernier match J2 → **test manuel du rappel à faire dans une autre session** (procédure en annexe A1 du plan `~/.claude/plans/j-ai-push-la-function-iridescent-pond.md`).
- [ ] **Fin de la checklist device** : test manuel du rappel H-1 (voir A1), pas de re-rappel au tick suivant, plus de rappel après avoir pronostiqué ; push résultats après passe 1 avec les bons points, rien à la passe 2 ; tap rappel → onglet Matchs, tap résultat → onglet Résultats. Déjà validé : APK dev build, permission au premier lancement, token en base, push de test via expo.dev/notifications.
- [x] Saisie admin des essais **J2** (`scripts/admin-set-tries.sql`) — faite pour les premiers matchs (J1 sans objet : aucun prono). Passe 2 vérifiée OK.
- [x] **Test live J2 du NC le 2026-07-11** — fait sur le premier dev build Android : « très content du premier rendu, pas de bug ». Standings + Realtime + nouveau DS validés en réel.
- [x] **Carte LIVE activée le 2026-07-13** (par Corentin) : secret `SYNC_LIVE_SECRET` + Vault `sync_live_secret`, EF `sync-live` déployée, migration cron poussée (`sync-live-5min` actif), clé Highlightly **Pro** en place. Vérifié : tick manuel → `200 {"skipped":true}` (early-exit hors match). S'activera au prochain match NC en direct.
- [x] **Rebuild du dev client iOS** — fait le 2026-07-13 pour vérif simulateur. ⚠️ **Il faut un prebuild `--clean`** (`npx expo prebuild --clean -p ios` puis `expo run:ios`, ou supprimer `ios/` avant) : un `expo run:ios` sur un `ios/` préexistant ne ré-applique PAS le config plugin `expo-image-picker` → `NSPhotoLibraryUsageDescription` manque → **crash TCC** au tap « Changer ». Pareil côté **Android** (`expo run:android`, prebuild clean) pour embarquer les modules natifs.
- [ ] **`supabase functions deploy delete-account`** (purge du dossier avatar ajoutée le 2026-07-13).
- [ ] Renouvellement de l'abonnement odds **Highlightly Pro** (échéance ~2026-08-06) : décider avant chaque compétition (6N, RWC) ; sans lui, `/odds` répond 401 et on retombe sur le fallback ×2.0. **Le score live in-play dépend aussi du plan Pro** — à garder à l'esprit pour la carte LIVE.
- [ ] Réévaluer la confirmation email (désactivée) au Lot 7

## Décisions clés (actées, ne pas re-débattre)

*Détail complet dans la mémoire projet (`trycast-project-charter`). Rappel des plus structurantes :*

- **Prono unique** par (user, match), partagé entre ligues ; points rétroactifs en rejoignant.
- Saisie = **score exact** + cases bonus offensif ; points vainqueur **pondérés par les cotes** (fallback 2.0), barème versionné en DB (`scoring_rules`).
- Deadline prono au **kickoff**, imposée par **RLS serveur**.
- **Scoring 2 temps** : passe 1 immédiate sans bonus offensif, passe 2 après saisie admin des essais (aucun fournisseur ne les expose — définitif MVP).
- Logique de scoring en **module TS pur** + écriture atomique via **une seule RPC** `apply_match_scores`.
- Source de données : **Highlightly** (API-Sports, Sportradar, etc. écartés).
- **4 onglets** (Lot 5.5, remplace les 5 du Lot 5) : Matchs / Résultats / Classement / Profil — les ligues sont ventilées (CTAs sur l'accueil, bascule Ligues/Général dans Classement, gestion via le détail de ligue).
- **Auto-save optimiste du prono** (Lot 5.5, remplace le bouton Valider) : statut À pronostiquer → Enregistrement… → Enregistré dans la carte.
- Ligues : création/adhésion **uniquement par RPC** (code d'invitation jamais requêtable) ; owner supprime son compte → sa ligue disparaît en cascade (transfert = v2) ; l'owner ne quitte pas sa ligue, il la supprime.
- **i18n : FR langue source via i18next** (clés typées, namespaces par domaine), `profiles.locale` synchronisée au démarrage pour les notifications du Lot 6 ; EN livré au plus tard avant la RWC 2027.
- **Design system** : identité vert bouteille/crème/grenat (« le grenat est une étincelle, jamais un fond »), Anton + Inter, light + dark complets. Référence : `docs/design/` + skill `trycast-design-system`.

## Points ouverts / dette assumée
- App lit encore `BAREME_V1` hardcodé pour l'aperçu → **brancher l'app sur la DB avant de créer une v2** du barème.
- Override npm `react-native-css` → `lightningcss@1.30.1` (bug bundling natif) — à réévaluer quand react-native-css > 3.0.7 sort.
- **Contrainte tokens natifs** (découverte 2026-07-09) : react-native-css n'enregistre pas les variables `:root`/`@theme` au runtime → les `--color-*` de `global.css` doivent rester des littéraux ou `light-dark(hex, hex)` sans `var()` interne ; hex dupliqués dans `src/tw/palette.ts` (couleurs lues en JS via `useThemeColor`), synchronisation verrouillée par `src/tw/palette.test.ts`.
- Barre « moi » épinglée du Classement : le rang du joueur au-dessus est approximé à rang − 1 (exact hors ex æquo exotiques, documenté dans `pinned-me-row.tsx`).
- **Plancher browserslist obligatoire** (fix 2026-07-10) : le champ `browserslist` de `package.json` (Chrome 123 / Safari 17.5 / Firefox 120 minimum) garantit que `light-dark()` survit à la passe Lightning CSS d'`@expo/metro-config` — le retirer ou le baisser re-casse silencieusement tout le dark natif. La bascule manuelle web passe par `color-scheme` sur `:root` (`theme-preference.ts`), plus de dépendance au « space toggle » `--lightningcss-*`.
- Tailwind : la palette par défaut (`bg-blue-600`…) reste techniquement disponible — la neutraliser (`--color-*: initial`) maintenant que tous les écrans sont sur les tokens.
- Identifiants encore hardcodés (signalés) : `package.json` (typegen `--project-id`), `app.json` (owner EAS, requis), migration cron `20260705000300` (URL projet).
- Base dev : 6 matchs J1 NC ont `scored_at` mais 0 prono scoré → classement général NC vide (état donnée, pas un bug).

## Journal des sessions
- **2026-07-14 (bis)** — **Audit natif des écrans + unification clavier/headers** (état des lieux demandé par Corentin — headers, safe areas, clavier, expo-blur, tabs custom vs natifs, react-native-screens — puis lots A/B/C validés en direct, lot D reporté). **Lot A (clavier)** : primitive `Screen` enrichie et adoptée partout où l'écran est un simple scroll — pattern clavier auth intégré (`automaticallyAdjustKeyboardInsets`, `keyboardShouldPersistTaps="handled"`, dismiss interactif iOS / on-drag Android), variantes `top` (`safe`/`none`) et `bottom` (`content`/`tabBar`) ; migrés : auth ×3, **Matchs** (le scroll des pronos porte les inputs de score — sticky dates conservées via pass-through), détail match, create/join (**KAV supprimé** : offset faux sous header natif), Réglages ; **la tab bar flottante s'escamote quand le clavier est ouvert** (fondu + glissé Reanimated, `pointerEvents` coupés — critique sur Android où la barre `absolute bottom-0` remonte au-dessus du clavier en mode resize) ; modale mot de passe sous `KeyboardAvoidingView` (pas de header dans une modale → pas de piège d'offset). **Lot B (headers natifs)** : thème navigation construit depuis `palette.ts` (`src/tw/navigation-theme.ts` — fond `bg`, card `surface`, texte/hairlines DS, Inter ; consommé par le ThemeProvider racine, il suit la bascule manuelle car `theme-preference` passe par `Appearance`) + `screenOptions` du stack (app) (header fond `bg` sans hairline = continuité visuelle, titre `Inter_600SemiBold`, **chevron couleur texte** — le grenat reste réservé aux CTA/live/sélection) ; **Réglages passe sous header natif** (titre 100 % natif, header custom chevron+Anton supprimé — décision : titre natif pour tous les écrans poussés, tant pis pour la maquette). **Lot C** : fond blanc en dur du stack auth supprimé (flash en dark — le fond des transitions vient désormais du thème), sheet des points calée sur la barre gestuelle (`max(inset.bottom, 32)`), **expo-glass-effect désinstallé** (jamais importé). **Décisions actées** : tab bar custom conservée (le `TabSlot` d'expo-router/ui passe déjà par `ScreenContainer`/react-native-screens avec détachement des onglets inactifs ; des tabs natifs rendraient la pilule signature impossible) ; pas d'expo-blur (surface quasi opaque déjà actée au DS) ; `Screen` = porteur unique du pattern clavier ; profil/classement/résultats (blocs épinglés + scroll interne, sans input) gardent leur layout avec `useScreenInsets` ; **lot D (`enableFreeze`) reporté**. Vérifié : typecheck/lint/format + **224 tests** ; **simulateur iPhone 17 Pro** : saisie de score → clavier number-pad, le scroll suit, **tab bar escamotée puis revenue au dismiss** (swipe interactif), join (bordure focus brand, header thémé), modale mdp entière (boutons compris) au-dessus du clavier, headers **light et dark** (bascule instantanée) ; thème Sombre de Corentin restauré, aucun prono modifié (focus sans saisie). **Piège documenté au skill simulateur** : clavier logiciel absent au focus = « Connect Hardware Keyboard » du simulateur → `defaults write com.apple.iphonesimulator ConnectHardwareKeyboard -bool false` + **redémarrage du sim** (laissé à `false` pour les tests ; ⇧⌘K pour retaper au clavier Mac). **Reste à valider sur l'Android réel de Corentin** : tab bar au clavier (le cas critique) et resize des écrans à inputs. 12 commits (`9b8aea6`→ + docs). Pas de push.
- **2026-07-13 (quinquies)** — **ResultCard à deux zones tactiles** (maquette mise à jour par Corentin, commit `b4dfa34` : panneau « Deux zones tactiles » dans `TryCast Resultats.dc.html`). La ligne pointillée devient une frontière d'interaction : **zone A** (heure/verdict + équipes/score) → page de détail du match ; **zone B** (ton prono + points gagnés + « Voir le détail des points ») → sheet du barème ; la partie basse **sans prono** navigue aussi vers le détail (zone A suite). Implémentation : `result-card.tsx` réécrit — l'unique `Pressable` englobant devient deux `Pressable` par zone (feedback press `scale-[0.985]` + teinte, `will-change-variable` dans les classes de base — piège react-native-css), carte en `overflow-hidden` avec padding porté par les zones ; **prop `onOpenMatch` optionnelle** : fournie par Résultats **et** Profil > Pronos (`router.push('/match/[id]')`), absente sur la page de détail elle-même (zone A inerte, sheet intacte) ; le bloc « La communauté a joué » reste un footer **non tactile** (hors maquette deux-zones, fonctionnalité conservée). Bonus de la maquette : la sheet du barème gagne la section « **Bonus pronostiqués** » (tags outline, clé `predictions:breakdown.bonusPredicted`). Au passage : l'export design `b4dfa34` a **remis la timeline** dans `TryCast Match Detail.dc.html` (écrase le retrait `358f6da`) — **assumé par Corentin** (le design sera remis à jour plus tard) ; la timeline reste hors MVP côté app, ne pas la porter.
- **2026-07-13 (quater)** — **Page de détail de match livrée (dernier bloc maquetté), timeline retirée du MVP.** Périmètre validé par Corentin en plan mode : porter toute la maquette `TryCast Match Detail.dc.html` **sauf la timeline** (abandonnée : aucune source de données d'événements, spike Highlightly), header natif, **entrée = carte LIVE uniquement** (pas de nav depuis les cartes prono ; **ResultCard intouchée**, Corentin maquette ça), membres sans prono listés avec « — », **pronos des autres jamais visibles avant kickoff** (bloc « Pronos masqués » avant). Backend : migration `20260713000100` — RPC `get_match_league_predictions(match, league)` `security definer` (seule porte vers les pronos d'autrui : garde `is_league_member` anti-énumération, **masquage serveur `now() >= kickoff_at`**, left join = membres sans prono en lignes null, username/avatar via profiles) ; `e2e-leagues.sh` +4 assertions (non-membre → vide, membre voit après kickoff, masquage avant kickoff, refus anon) ; `e2e-predictions.sh` en non-régression ; advisors = seul le WARN security definer assumé. App : fragment `MATCH_WITH_TEAMS` extrait dans `match-select.ts` (dédup), hooks `useMatch(id)` (embed teams+competition, `maybeSingle`, **refetch 60s seulement si `in_play`** — bascule seule en terminé) et `useMatchLeaguePredictions` (enabled seulement après kickoff), module pur testé `match-phase.ts` (upcoming/locked/live/finished — `scheduled`+kickoff passé = locked, lag du sync) ; composants `match-hero` (score live/final ou coup d'envoi, cotes en pills `Intl`, chip intégré), `match-status-chip` (dot pulsante Reanimated, `useReducedMotion`), `locked-prediction-card` (read-only : PredictionCard non adaptable, tout son corps est de la saisie), `masked-predictions`, `member-prediction-row` (« Score exact » dérivé client, `+pts` après scoring) ; écran `match/[id].tsx` (header natif, 3 phases : PredictionCard / LockedPredictionCard / ResultCard, Select de ligue si >1 — note « Une seule ligue » sinon, segmented Pronos/Classement réutilisant `LeaderboardRow`+`markTies`, CTA 0 ligue, skeletons + introuvable) ; carte LIVE pressable → détail. Fix au passage : `Stack.Screen name="(tabs)" options={{ title: '' }}` — le back natif affichait le libellé technique « (tabs) ». Maquette nettoyée de la timeline (JSX, mocks, flags, frame `noevt`). Vérifié : typecheck/lint/format + **224 tests** (+4 match-phase), e2e leagues/predictions verts, **simulateur iPhone 17 Pro : 3 états × light et dark** (deep links + tap carte LIVE réel, match forcé `in_play` en SQL puis **restauré**, thème Sombre de Corentin **restauré**). Pièges : nouvelles routes typées → `router.d.ts` (gitignoré) régénéré en lançant Metro quelques secondes ; après `terminate`, un `openurl` retombe sur le **launcher du dev client** (retapper le serveur). Fausse alerte investiguée puis classée : l'engrenage flottant en haut à droite des screenshots n'est **pas** le bouton Réglages du Profil mais le **FAB natif d'expo-dev-menu** (`gearshape.fill`, propre UIWindow au-dessus de l'app, dev builds uniquement — absent en release) ; documenté dans le skill simulateur. 9 commits (`328cde5`→`58ea834`). Pas de push.
- **2026-07-13 (ter)** — **Animations de coulissement « magic pill » (Reanimated 4)**, à la demande de Corentin. **(1) Tab bar** (`src/components/app-tabs.tsx`) : la pastille active — jusque-là un fond statique par onglet — devient un **élément UNIQUE qui coulisse** (ressort amorti `withSpring`, damping 26 / stiffness 240) vers l'onglet actif ; icône + label fondent vers le grenat par **cross-fade d'opacité** de deux couches empilées. Index actif via `usePathname()`, **géométrie mesurée par `onLayout`** (pas d'arithmétique gap/padding). **(2) Contrôle segmenté** (`src/components/ui/segmented-control.tsx`, primitive partagée → profite d'un coup au classement Ligues/Général, au profil, aux réglages thème et à l'auth) : même principe mais **glissé smooth sans rebond** (`withTiming` 220 ms ease-out), sur demande explicite. **Décision technique clé** : cross-fade d'opacité plutôt qu'`interpolateColor` car `useThemeColor` renvoie une **`var()` CSS non interpolable sur web** ; robuste natif + web. Les deux : anti-flash (pose sans glissé au 1er rendu via ref `firstPlacement`), respect de **« réduire les animations »** (`useReducedMotion` → bascule instantanée), pattern maison respecté (pas de `className` sur `Animated.View`, style animé + `View` enfant tokenisée). Reanimated **4.5.0** (et non « 3 » comme demandé — sans incidence) + `react-native-worklets` déjà en place, React Compiler compatible, **aucune dépendance ni rebuild natif**. Vérifié : typecheck/lint/format + **220 tests**, simulateur iPhone 17 Pro **light et dark** (glissé capturé **en vol** Matchs→Classement et Ligues→Général, alignement parfait, arrivée nette) ; état de test restauré (thème Sombre, onglet Matchs, segment Ligues). 2 commits : `88a123d` (tab bar), `3193351` (segmenté). Pas de push. Réglages faciles pour affiner le ressenti : `PILL_SPRING` (tab bar), `SLIDE.duration` (segmenté).
- **2026-07-13** — **3 blocs maquettés en attente de backend traités** (mot de passe, photo de profil, carte LIVE). Cadrage par 3 explorations parallèles (inventaire mocks = code quasi tout branché, backend existant, dashboard) → périmètre validé par Corentin : les 3 blocs actionnables. Livré : **(1) Changement de mot de passe** — pur client `auth.updateUser`, modale + rangée Réglages, réutilise `validatePassword`/`toAuthMessageKey` du domaine auth (aucun nouveau mapper). **(2) Photo de profil** — migrations `20260712000100`→`000400` (colonne `avatar_url` + grant colonne, bucket `avatars` public, policies insert/update/delete/**select** « son dossier », RPC leaderboards + `avatar_url`), `Avatar` avec photo (repli initiales) partout, upload/compression client (`expo-image-picker`/`-manipulator` 512px), purge dossier dans EF `delete-account`, `scripts/e2e-avatars.sh`. **Piège débogué en profondeur** : upsert **et** remove Storage font un SELECT d'existence sous RLS → policy SELECT « son dossier » indispensable (sinon 403 malgré policies écriture correctes) ; isolé via policy diag permissive + logs Storage. **(3) Carte LIVE (prépa code)** — colonnes `live_*` (distinctes de `home_score`), EF `sync-live` (transform pur testé), `live-match-card.tsx` + `use-live-matches` (polling 60s) + `live-period.ts`, i18n `matches:live.*`. **Vérifié** : typecheck/lint/format + **220 tests** (+8), `e2e-avatars.sh` OK (RLS dossier propre vs autrui, colonne lisible/protégée), advisors sans nouvelle ERROR, migrations `db push` + typegen. **Vérifié en simulateur** (iPhone 17 Pro, dev build rebuild `--clean`, thème Sombre inchangé) : mot de passe (sheet + erreur « ne concordent pas » sans appel serveur), photo de profil **round-trip complet** (permission FR → picker → recadrage → upload Storage → avatar affiché → Supprimer → retour initiales, base `cohozen` nettoyée avatar_url=null / 0 objet), accueil Matchs rendu sans carte LIVE (aucune donnée live). **Découverte** : le rebuild doit être un prebuild `--clean` sinon `NSPhotoLibraryUsageDescription` manque (crash TCC). Carte LIVE ensuite **activée par Corentin** (secret + Vault + deploy `sync-live` + db push cron + clé Highlightly Pro) et vérifiée : tick manuel via secret Vault → `200 {"skipped":true}` (EF OK, secret OK, early-exit hors fenêtre live) ; s'affichera au prochain match NC en direct. 9 commits (`f859a0c`→`8ef187d`). **Actions Corentin en attente** : rebuild dev client, deploy `sync-live` + `delete-account`, secret `sync_live_secret`. Pas de push.
- **2026-07-12 (bis)** — **Tokens couleurs DS v2 : surfaces neutres (charbon/sable).** Corentin a mis à jour le DS sur Claude Design et appliqué l'export dans `docs/design/` ; répercussion dans l'app. Changement : les **surfaces** passent de vert/crème à des **neutres chauds** — sable/blanc en light, **charbon chaud en dark** (fini la base verte) ; le **vert devient la marque** (`brand` : `green-400` #43a56e lisible sur charbon — avatars/icônes), le grenat reste l'étincelle inchangée ; `on-accent` → blanc. Comme tout l'UI passe par les tokens sémantiques, le re-thème est automatique : seuls **3 fichiers app** touchés — `src/global.css` (12 tokens `--color-*` + 2 primitives `ink` + commentaires v2), `src/tw/palette.ts` (miroir, garde-fou `palette.test.ts`), `src/components/app-tabs.tsx` (TabBar flottante : vert dark → pastille active teintée `accent/15` + hairline `white/12`, conforme aux tokens `--glass-*` du DS et aux captures `tabbar*.png`). Docs alignées : skill `trycast-design-system` (règle d'identité « surfaces neutres, vert = marque »), `AGENTS.md`. Vérifié : typecheck/lint/format + **212 tests** (dont parité CSS↔TS), simulateur iPhone 17 Pro en **light et dark** (fond sable / charbon, avatars & Langue en vert marque, TabBar grenat sans vert résiduel) ; **préférence de thème de Corentin = « Clair »** (et non « Sombre » comme le notait le skill simulateur), restaurée après test. 2 commits : `070a6cd` (app), `34a8322` (export DS). Pas de push.
- **2026-07-12** — **Config iOS locale (dev build) — parité avec Android.** Le lot Android avait ajouté `expo-dev-client` : dès lors `expo start --ios` (`npm run ios`) n'ouvre plus Expo Go mais attend un dev build installé sur le simulateur — d'où l'impression que « lancer sur simulateur iOS » ne marchait plus. Constat vérifié au git blame : **aucune config iOS n'avait jamais été retirée** (la section `ios` d'`app.json` s'est toujours limitée à l'icône) ; Android a *gagné* de la config, iOS était resté en retrait. Choix acté avec Corentin : **dev build local iOS** (pas Expo Go). Livré : `app.json` → `ios.bundleIdentifier` = `com.cohozen.trycast` (miroir de `android.package`) + `supportsTablet: false` ; `package.json` → scripts `ios`/`android` passés à `expo run:ios`/`expo run:android` (dev build natif ; `expo run:ios` a régénéré `/ios` gitignoré et réécrit ces scripts au passage) ; `eas.json` → profil `development` iOS `simulator: true` (parité de l'apk Android) ; skill `trycast-ios-simulator` mis à jour (dev build au lieu d'Expo Go, deep links `trycast://<route>`, bundle id `com.cohozen.trycast` pour `terminate`/`openurl`). **Prérequis env** : CocoaPods était absent (`brew install cocoapods`) pour l'étape pod install de `expo run:ios` ; Xcode 26.6 déjà présent. `app.config.ts` inchangé (il fait `...config` puis n'override qu'`android`). iOS/APNs sur *device* physique restent au Lot 7. Vérifié : typecheck/lint/format + **212 tests**, `expo config --type public` confirme `ios.bundleIdentifier`.
- **2026-07-11 (quater)** — **Blocs maquettés débloqués : courbe « Points par journée » + distribution communautaire 1/N/2** (plan `~/.claude/plans/j-ai-push-la-function-iridescent-pond.md`). Cadrage lancé par 3 explorations parallèles (notify, inventaire blocs, veille APIs live) + 1 agent de design. **Décisions actées par Corentin** : (1) la **distribution est visible pendant toute la période de pronos**, même avant kickoff (assumé, pour le peps ; remplace la note « à n'exposer qu'après kickoff ») ; (2) **GO du spike score live** — la clé Highlightly **Pro** renvoie bien `state.score` pendant le match (décalage 3-5 min), le « null » de juillet venait de la clé FREE ; **SportDevs confirmé inaccessible**, API-Sports inutile pour le live ; essais toujours en saisie admin. Livré : **Bloc A** (courbe) 100 % client — module pur `compute-points-by-round.ts` + chart SVG `points-trend-chart.tsx`, aucun backend (données déjà en cache) ; **Bloc B** (distribution) — migration `20260711000400_prediction_distributions.sql` (RPC `get_prediction_distributions` `security definer`, sans filtre kickoff), hook `use-community-distributions` (staleTime Infinity), helper pur `distribution-percentages` (plus fort reste → somme 100), composant `community-distribution` (N total affiché, issue jouée surlignée), barres communautaires **remplaçant** les barres de cotes sur PredictionCard (repli cotes si 0 prono) + bloc sur ResultCard (Résultats + Profil). Vérifié : **212 tests** (+8), typecheck/lint/format, `e2e-predictions.sh` **10/10** (+2 : la RPC agrège sans fuiter les lignes, refus anon), advisors = seul le WARN `security_definer` assumé (aucune ERROR `security_definer_view` — c'est pourquoi RPC et pas vue), simulateur iPhone 17 Pro en **light et dark** (courbe J1=0→J2=76 pts avec badge, distribution « 2 100% · 1 prono » surlignée grenat) ; thème « Sombre » de Corentin restauré. Reportés (plans prêts) : EF `sync-live` + carte LIVE (annexe A2), photo de profil (A3), test manuel notify (A1).
- **2026-07-11 (ter)** — **Mise en route du Lot 6 par Corentin + diagnostic du cron muet.** Corentin a tout déroulé : clé FCM uploadée aux credentials EAS, `GOOGLE_SERVICES_JSON` en env EAS, `NOTIFY_SECRET` + Vault, EF `notify` déployée, cron poussé (`notify-10min` actif), **premier build EAS Android validé** (APK sur son téléphone, token en base — commit `expo-dev-client` de son cru). Vérification serveur derrière : les ticks notify prenaient un **401 « Missing authorization header »** — l'EF était déployée avec `verify_jwt = true`, car `supabase/config.toml` ne déclarait pas `[functions.notify]` (contrairement aux EF sync) et la doc de mise en route ne mentionnait pas ce point. Fix : bloc `verify_jwt = false` ajouté à config.toml ; **le redeploy reste à faire par Corentin** (bloqué en mode auto, urgent avant la J2 du soir). README : section development build (push absents d'Expo Go, commandes EAS, `google-services.json` non versionné, test manuel via expo.dev/notifications).
- **2026-07-11 (bis)** — **Lot 6 livré (code) : notifications push, Android d'abord** (13 commits, plan `~/.claude/plans/parfait-tout-a-maintenant-resilient-ritchie.md`). Décision : **Android en premier** (téléphone perso de Corentin, pas de compte Apple Developer payant, APK direct) — Firebase `trycast-90446` créé par Corentin, package `com.cohozen.trycast` ; iOS/APNs au Lot 7. Backend : tables `push_tokens` (unicité sur le token, écriture **par RPC security definer** `register/unregister_push_token` — l'upsert PostgREST ne peut pas réaffecter la ligne d'un autre compte sous RLS, cas « changement de compte sur le même téléphone »), `notification_prefs` (booléens par type, absence de ligne = tout activé), `notification_sends` (dédup **claim-first** sur unique (user, match, type) : crash = notif perdue, jamais doublée ; supporte les receipts Expo) ; RPC `notify_reminder_targets`/`notify_result_targets` (SQL versionné, service_role only) ; **une seule EF `notify`** scannante (receipts → rappels H-1 → résultats passe 1, tokens morts DeviceNotRegistered purgés aux deux niveaux, `job_runs` avec early-exit) sur cron `3-53/10 * * * *` (décalé de 3 min après sync-results ; migration cron commitée **non poussée** — deploy EF d'abord). App : `expo-notifications` + `app.config.ts` (googleServicesFile via env EAS, fichier gitignoré), `eas.json`, feature `src/features/notifications/` (garde-fou `push-support` — Expo Go/simulateur/web court-circuités —, permission demandée une fois au premier lancement connecté, token désenregistré avant signOut), deep links au tap via **allowlist** `data.url` → onglets Matchs/Résultats (`getLastNotificationResponseAsync` + listener), primitive `ui/Switch` (46×28), section Notifications des Réglages (master + sous-titre d'état + 2 toggles + bannière permission refusée avec `Linking.openSettings()`, re-check au retour au premier plan, upsert optimiste). Vérifié : suite complète (**204 tests**, +24), `e2e-notifications.sh` **11/11 vert contre le dev** (réaffectation de token, isolation RLS, tables/RPC serveur verrouillées), advisors = pattern assumé uniquement, `expo export` iOS OK, simulateur iPhone 17 Pro (section en dark **et** light, master off → sous-toggles masqués + `master=false` en base, état de Corentin restauré : thème Sombre, prefs tout-ON, 0 token). Reste : mise en route par Corentin (checklist dans « En attente »).
- **2026-07-11** — **Troisième série de retours Lot 5.5** (6 retours, dernière passe avant le Lot 6 ; 4 commits). Résultats : la bande de jours présélectionne le **jour avec matchs le plus proche d'aujourd'hui** (dernier de la plage, bornée à today) et se positionne dessus au montage (`contentOffset` calculé depuis la géométrie des pilules) ; la modale de détail des points affiche la rangée **« Points de base · 1 N 2 »** vue pendant les pronos (`winnerPointsByOutcome`, issue pronostiquée encadrée grenat) au-dessus du barème. Profil : `computeProfileStats` reçoit désormais les matchs terminés — les **non pronostiqués comptent au dénominateur de la précision** (choix Corentin : catégorie dédiée, segment neutre `text-faint` dans le donut + 4ᵉ ligne de légende, `missed` reste distinct) ; l'onglet Pronos liste **tous les matchs terminés** y compris sans prono (ResultCard gérait déjà le cas) ; bouton réglages sur la **même row que avatar/pseudo** (visible aussi pendant le skeleton) ; petit `pb-2` sous le bloc épinglé des tabs. Tests de `compute-profile-stats` réécrits (Map + matchs, 5 cas). Vérifié : suite complète (180 tests) + simulateur iPhone 17 Pro en **light et dark** (strip sur SAM 11, modale cohérente carte prono — ×1,32 → 13 pts, donut 25 % = 2 bons / 8, les 6 matchs du 4 juillet en « non pronostiqué » listés) ; thème « Sombre » de Corentin restauré après le test light.
- **2026-07-10 (sexies)** — **Fix : warning react-native-css « added or removed a variable » au press des boutons** (remonté par Corentin sur créer/rejoindre ligue). Cause : les classes ajoutées conditionnellement au press (`scale-[0.97]`, `shadow-none`…) compilent en variables CSS (`--tw-scale-*`, `--tw-shadow`) ; react-native-css fige au premier rendu le statut « porteur de variables » d'un composant, et quand il bascule au press, il **démonte/remonte le composant et ses enfants** (état perdu + warning dev). Fix : classe runtime `will-change-variable` (fournie par react-native-css, inerte sur web) dans les classes de base du `Pressable` des 3 primitives concernées (`button`, `icon-button`, `chip`) — le statut ne bascule plus jamais. Piège documenté dans le skill DS. Vérifié : suite complète (177 tests) + simulateur iPhone 17 Pro (press réel d'un `Button` actif : zéro warning dans Metro, feedback rétrécissement/assombrissement intact — screenshot en plein press). Note : `format:check` échoue sur `src/app/(app)/(tabs)/index.tsx`, fichier en cours de modif par Corentin (hors périmètre, non touché).
- **2026-07-10 (quinquies)** — **Safe areas généralisées** (suite du retour tab bar) : hook `useScreenInsets()` dans `src/tw/use-screen-insets.ts` — `top` = `max(inset haut, 56)` (remplace tous les `pt-14`), `bottomTabBar` = `94 + max(inset bas, 16)` (remplace tous les `pb-32`) ; planchers = anciennes valeurs fixes, donc rendu identique sur les appareils déjà calibrés, et plus de risque de chevauchement sur les barres d'état Android hautes. Migré : les 4 écrans à onglets (y c. états loading), Réglages, les 2 écrans auth, la primitive `Screen` ; les écrans `league/*` gardent leur header natif (safe area déjà gérée par le Stack). **Piège react-native-css découvert en route** : hors cible `style`, une prop inline écrase le style dérivé des classes — passer `contentContainerStyle` faisait perdre tout le `contentContainerClassName` (px-5/gap/max-w envolés au premier essai). Fix centralisé : le wrapper `ScrollView` de `@/tw` fusionne lui-même classes résolues + inline (garde-fou dev si l'élément est enveloppé par un provider). Documenté dans le skill DS. Vérifié : suite complète (177 tests) + parcours simulateur iPhone 17 Pro (5 écrans, sticky et réserve basse intacts) ; pas de simulateur SE sous iOS 26 — planchers garantis par construction (`Math.max`), Android à valider sur appareil réel.
- **2026-07-10 (quater)** — **Deuxième série de retours Lot 5.5** (6 retours d'ergonomie, 5 commits). Auth : contenu aligné en haut (`justify-center` retiré — le switch Connexion/Inscription ne fait plus sauter logo + tabs) et champ actif maintenu visible au-dessus du clavier (`KeyboardAvoidingView` remplacé par `automaticallyAdjustKeyboardInsets` sur le ScrollView ; Android compte sur le mode `resize` par défaut — **smoke test Android à faire par Corentin**). Tab bar flottante décollée de la barre système via `useSafeAreaInsets` (premier usage de safe-area-context du projet ; provider déjà fourni par expo-router ; plancher 16px conservé). « Mes matchs » : en-tête + carte stats épinglés (motif `View flex-1` > bloc `flex-none` > ScrollView, repris de Résultats) et **dates sticky** via `stickyHeaderIndices` (groupes aplatis en enfants directs, en-têtes wrappés `bg-bg py-1.5` opaques) — pas de SectionList (contenu hétérogène, volumétrie faible). Profil : tout le bloc identité → tabs épinglé, seul le contenu d'onglet défile (le `Select` en Modal RN ne pose aucun souci dans un bloc fixe). Vérifié : suite complète (177 tests) + parcours simulateur iPhone 17 Pro (switch tabs auth immobile, scroll clavier, sticky dates poussées entre journées, 3 onglets Profil, tab bar au-dessus de l'indicateur home). Au passage : le fix clavier QWERTY du simulateur n'avait **pas** persisté — rejoué (recette du skill).
- **2026-07-10 (ter)** — **Fix : le dark mode ne s'appliquait quasiment pas en natif** (iOS + Android — seuls les icônes/chrome JS et les variants `dark:` changeaient). Cause racine : avant que react-native-css compile le CSS, la passe Lightning CSS d'`@expo/metro-config` (cibles browserslist **par défaut**, faute de config) réécrivait chaque `light-dark()` de `global.css` en « space toggle » `var(--lightningcss-light/dark)` — or react-native-css ne génère sa règle dark native (résolue via `Appearance`) que s'il reçoit un vrai nœud `light-dark()`. Fix minimal : **champ `browserslist` dans `package.json`** (plancher Chrome 123 / Safari 17.5 / Firefox 120 = support natif de `light-dark()`) ; la bascule manuelle web de `theme-preference.ts` migre du hack `--lightningcss-*` vers la propriété standard `color-scheme` sur `:root`. Au passage, vérifié que l'optimize prod de Tailwind (`@tailwindcss/node`) **exclut** le lowering LightDark → rien à changer côté postcss. Vérifié : sonde de compilation (9 → **83** règles `prefers-color-scheme` dans la stylesheet native, hex dark présents), 177 tests + typecheck/lint/format, `expo export` iOS, simulateur (dark système, bascule Clair/Sombre/Système en live, préférence « Sombre » de Corentin restaurée), web (tokens suivent `prefers-color-scheme`, forçage manuel OK, console propre). Limitation pré-existante inchangée (pas une régression) : sur web, le forçage manuel n'affecte pas les surfaces `@media (prefers-color-scheme)` (`dark:`, `.tc-shadow-*`, JS `useColorScheme`).
- **2026-07-10 (bis)** — **Fix natif : texte éclaté sur « Rejoindre une ligue »**. La phrase d'intro s'étalait sur toute la hauteur de l'écran (iOS) : react-native-css résout `line-height: var(…)` au runtime en multipliant le nombre par la taille de police, donc les `--text-*--line-height` en px de `global.css` devenaient des multiplicateurs (22px → line-height 330). Touchait tous les écrans utilisant l'échelle `text-body` (join, create, forgot-password). Fix : line-heights de l'échelle en **ratios sans unité** (mêmes px en web et natif), piège documenté dans le skill `trycast-design-system`. Vérifié au simulateur (join + create, light/dark) + `expo export` iOS + suite complète. Dans la foulée : les 3 tests rouges de `day-range.test.ts` alignés sur l'**ordre chronologique** de la bande de jours (plus ancien à gauche → plus récent à droite, décision UX de Corentin actée dans `deb4a00` ; c'étaient les tests qui étaient en retard, pas le code). Vérifié au simulateur : SAM 4 → VEN 10, aujourd'hui cerclé à droite.
- **2026-07-10** — **Outillage : pilotage du simulateur iOS par Claude**. Installation d'AXe (`brew tap cameroncooke/axe`), workflow validé de bout en bout sur iPhone 17 Pro (iOS 26.5) : boot + Metro en tâche de fond + Expo Go, screenshots lus par Claude, arbre d'accessibilité (`axe describe-ui`), taps (navigation par tabs), saisie clavier, deep links Expo Router (`exp://localhost:8081/--/<route>`, recharge le bundle). Piège corrigé : clavier AZERTY du simulateur faussait `axe type` → clavier QWERTY seul (persistant). Recette complète dans le skill `.claude/skills/trycast-ios-simulator/`.
- **2026-07-09 (bis)** — **Correctifs des premiers retours Lot 5.5** (8 retours). Cause racine des 3 plus gros : react-native-css n'enregistre pas les variables CSS au runtime natif → dark jamais appliqué, icônes tab bar sans couleur, `placeholderTextColor: var(...)` invalide (erreurs sur les écrans à champ de saisie). Fix : tokens `@theme` en littéraux `light-dark(hex, hex)`, hook `useThemeColor` + `src/tw/palette.ts` (test de synchro CSS↔TS), suppression de `useCSSVariable`. Aussi livrés : points potentiels des 3 issues 1/N/2 + barres de probabilité (module pur `potential-by-outcome`), pseudo déplacé dans Réglages (section Compte, mapper d'erreurs profil dédié), primitive `Select` (listbox ancrée), **refonte Profil** (chiffres clés, sélecteur compétition, tabs Stats/Pronos/Ligues, donut, rang via `use-my-rank` sans nouveau backend), **refonte Classement** (Select ligue, podium 2/1/3, ex æquo, barre « moi » épinglée), **bande de jours continue** sur Résultats (jours vides désactivés, aujourd'hui cerclé). Vérifié : 177 tests, `expo export` iOS OK, parcours web light+dark complet (thème dark et forçage manuel validés visuellement). **Reste : valider sur mobile réel à la J2.**
- **2026-07-09** — **Lot 5.5 livré (V0)**. Livrable Claude Design versionné dans `docs/design/` ; tokens Tailwind v4 light/dark (`light-dark()`), polices Anton/Inter, primitives `src/components/ui/` ; infra i18next (FR source, clés typées, migration `profiles.locale` + sync au login) ; passe écran par écran : auth unifiée (Connexion/Inscription segmentée), accueil Matchs maquette (mini-dashboard, compteur, auto-save), Résultats, Classement (bascule Ligues/Général), **navigation 4 onglets**, écrans ligue, Profil + **écran Réglages** (thème système/clair/sombre persisté, modale de suppression avec re-saisie du pseudo). Arbitrages Corentin : 4 onglets, auto-save, light+dark dès V0, Réglages minimal, blocs data reportés (live, distribution 1/N/2, stats par compétition, Match Detail — détail dans « blocs maquettés en attente de backend »). Vérifié web light+dark + login réel ; **reste à valider sur mobile à la J2**.
- **2026-07-07** — Cotes validées en réel : abonnement Highlightly Pro pris (1 mois), Corentin a vérifié que les runs de sync d'hier ont bien récupéré les odds ; pronos saisis pour la J2. Feuille de route : ajout du **Lot 5.5 (design system + i18n)** avant le push, stratégie i18n actée (FR source, `profiles.locale`, EN avant RWC).
- **2026-07-05** — Création du DASHBOARD. État : lots 0-5 livrés, Lot 6 (push) prochain.
